# v0.3.4-dev Cloud hard acceptance

## 1. 当前状态

`v0.3.4-dev` 只是未封板的 Source/Candidate：本地 contract cleanup 获批，Cloud hard acceptance 尚未开始。
bootstrap 的 ZIP SHA-256 为 64 位 zero hash并 fail closed；不存在 tag、Release 或 Published Release 资产。

| Gate | 状态 |
|---|---|
| Local contract cleanup | IN PROGRESS |
| Source/Candidate Cloud | PENDING |
| Stable seal/publication | NOT AUTHORIZED |
| Published Release Cloud | NOT AUTHORIZED |
| Latest/rollback promotion | NOT AUTHORIZED |
| Product Phase 4 | NOT AUTHORIZED |

## 2. 本 gate 边界

- 删除 runtime bundle 中不参与 source/build/install/runtime 决策的历史 programme metadata。
- 保持四个 upstream runtime、两个 owned runtime、installer inventory、Host ABI 与 production dispatch 不变。
- 以 exact current id/source inventory guard 阻止 Phase 4+ script 未经独立 contract gate 准入。
- 不删除 `ledger-summary.sh`，不合并 `upstream-manifest.json` 与 runtime bundle。

## 3. 后续 Cloud 入口

真正进入 Cloud gate 时，必须从
[`Cloud hard acceptance template`](cloud-hard-acceptance-template.md) 复制完整双通道协议，冻结当时 exact
source、candidate ZIP、测试结果和失败证据。当前文件不得预填 R5-SC/R5-PR PASS、公开 URL、资产 SHA 或
最终验收通过标记。
