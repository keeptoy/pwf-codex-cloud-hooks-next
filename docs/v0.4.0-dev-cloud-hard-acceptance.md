# v0.4.0-dev Cloud hard acceptance

本文件只描述 `v0.4.0-dev` 的版本验收增量。稳定执行协议、黑盒提示词、停止条件和 evidence schema 只见
[`Cloud hard acceptance template`](cloud-hard-acceptance-template.md)；实际验收完成后再写入不可变证据。

F1B Source/Candidate Cloud hard acceptance 已完成，并严格绑定下述 exact zero-hash candidate evidence。development bootstrap 使用 64 位 zero hash 并
fail closed；它只能通过显式 candidate ZIP URL/SHA override 安装，不代表 tag、Release 或 Published
Release 资产。

<a name="v0-4-0-dev-acceptance-scope"></a>

## F1B 验收增量（historical gate instance）

- F0 建立 `v0.4.0-dev` package/Release identity、外部 bootstrap 和 candidate/accepted 治理窗口；F1A
  原子迁移 source manifest schema 4、runtime bundle v2、Release artifact v2、adapter/四项 ABI
  placement 与 entry-owned mode。
- F1B 只轮转 plan request/result v2；catch-up 继续使用 v1。adapter producer 与 owned runtime capability
  均固定为 `allowed_profiles=[legacy]`，Host 输出、canary、上下文顺序和 managed-legacy 行为必须保持不变。
- `.mode` reader/normalizer 作为 inactive seam 随 runtime 安装，但 production 不得调用；nonce、attestation
  和 ledger reader 不得存在。Linux suite 必须零 skip 执行 future-profile refusal、legacy zero-read、unsafe
  mode/link/race 与旧 marker 不影响输出等证明。
- candidate installer 只接受当前完整状态或 transition contract 精确列出的 v0.3.5 predecessor。exact forward
  migration、tamper-before-write refusal 与 clean rollback 已由 ref-aware 本地 oracle 验证；tagless
  Source/Candidate Cloud 不伪造 refs，也不把 publication-only oracle 改写成 no-live 结果。
- exact current id/source inventory guard 继续拒绝未准入文件；Cloud setup 必须从当前 manifest/bundle 动态
  验证 v2 paths、hash、entry count、installed inventory 与 executable mode，不手填历史数量。
- 本次验收增量没有改写 B～E 黑盒提示词；它们继续验证真实 Host 行为与 legacy baseline 等价，F1B 的
  新增证明来自 Linux 零 skip suite 和 candidate 安装后的 exact deep check。

## F1B 执行范围（historical gate instance）

本次只执行通用模板的 Source/Candidate/no-live 路线，顺序固定为：

1. [`4.1 Source/Candidate setup`](cloud-hard-acceptance-template.md#source-candidate-setup)
2. `5.1 B-SC post-install Resume`
3. `6 C canonical baseline`
4. `7 D canonical UserPromptSubmit`
5. `8.1 E1 long-tail`
6. `8.2 E2 real Resume`
7. [`9.1 Source/Candidate deep check`](cloud-hard-acceptance-template.md#source-candidate-deep-check)

不得执行 Published Release 通道 `4.2`、`5.2`、`9.2`：当前 candidate 未 seal、未发布，也没有 Published
Release 身份。验收结果必须如实记录 setup、B～E 黑盒观测和 deep check；Linux test runner 必须显示
`0 failed`、`0 skipped`。

该 F1B development acceptance 不授予 F2A/F2B activation、managed token 生效、seal、publication、
promotion 或 baseline rotation。后续 gate 必须由新的活动 task plan 单独授权。

<a name="v0-4-0-dev-source-candidate-evidence"></a>

## F1B Source/Candidate evidence (historical gate instance)

维护者在全新 no-live Cloud 环境按 `4.1 → 5.1 → 6 → 7 → 8.1 → 8.2 → 9.1` 完整执行并回传：

- exact source：`fb61ca74517de399e80835a38ef4b2444d9e6468`
- Linux portable suite：133 tests，133 pass，0 fail，0 skipped
- deterministic candidate ZIP：两次独立构建字节一致；22 entries；80,800 bytes
- candidate ZIP SHA-256：`1866d56503a38dbd8269362c475c63ba1f4638ab7f69f66bad3b6ac9d5653dc9`
- setup：`PWF_SOURCE_CANDIDATE_SETUP=PASS`
- post-Resume：doctor `healthy=true`、`repairable=false`、`managed=true`，无 errors/blockers
- installed state：12 runtime files、4 pristine upstream files、bundle inventory authoritative、adapter-only policy
- residue：`SNAPSHOT_LEFTOVERS=0`，`PWF_SC_POST_RESUME=PASS`
- B～E no-live 黑盒路线已按模板完成；执行后 source worktree clean

结论：`SOURCE_CANDIDATE_CLOUD_PASS / F1_FOUNDATION_COMPLETE`。该结论只证明 exact zero-hash
Source/Candidate，不产生 Published Release identity，也不授权 F2A/F2B、seal、publication、promotion 或
baseline rotation。
