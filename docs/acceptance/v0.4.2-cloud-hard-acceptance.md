<a name="v0-4-2-release-operator-guide"></a>

# v0.4.2 Cloud hard acceptance

本文件是single-Discovery `v0.4.2`的Release operator guide。它编排Source/Candidate与Published Release两个独立通道。
Source/Candidate、第一轮真实退役、immutable publication、Published Release Cloud、GitHub Release Latest promotion confirmation、
第二轮退役与final Post-run/C2均已完成。

本版本没有新增product/runtime行为。要验证的是：文档治理delta已经收敛为一个可复现的`0.4.2`候选，exact v0.4.1
predecessor仍可安全进入未改变的installed runtime，最终公开下载字节与Source/Candidate实际验收字节一致。

执行关系：

```text
candidate admission preflight（已闭合；只读、不删除）
  → C0 / Source-Candidate Cloud（PASS）
  → source-candidate closeout retirement checkpoint（已闭合；无删除）
  → C1 / channel checkpoint与第一退役证据（本次状态commit）
  → tag精确指向C0，维护者完成immutable publication（已闭合）
  → Published Release Cloud（PASS）
  → GitHub Release Latest promotion confirmation（已闭合）
  → role-window closeout checkpoint（PASS）
  → C2 / final Post-run与programme closeout（COMPLETE）
```

本guide不创建standing Phase 9，不使用P9-A～F编号，也不授权维护者之外的远端写操作。
上述宏观顺序来自[`ROADMAP`的Release四步与C0/C1/C2](../../ROADMAP.md#release-four-step-flow)和
[`ROADMAP`的两个retirement checkpoint](../../ROADMAP.md#version-train-two-retirement-reviews)；Latest晋级判断只读
[`GitHub Release Latest promotion confirmation`](../../ROADMAP.md#github-release-latest-promotion-confirmation)。本guide只保存v0.4.2的
exact输入、教程、channel checkpoint与最终证据，不重新定义通用Release流程。

## 1. 定位与验收 claim

- 版本列车：`v0.4.2` documentation-governance Release closeout；不新增Product Phase。
- Discovery claim：Release/acceptance/history治理形成的仓库delta可以作为stable patch候选交付，同时保持v0.4.1已接受行为、
  exact predecessor admission与public download identity闭合。
- Source/Candidate证明final source、portable Linux suite、deterministic ZIP、override安装和真实Resume lifecycle。
- Published Release证明维护者发布的immutable bootstrap/ZIP默认下载链、Fresh startup与公开字节deep check。
- PASS不自动授权publication、Latest、下一Product Phase或任何production/runtime扩面。

## 2. Exact inputs 与前置条件

| Input | Exact value / authority | Admission check |
|---|---|---|
| candidate source | `0.4.2` branch上包含本guide及全部候选输入的C0；完整hash由本地handoff命名为`SOURCE_CANDIDATE_HEAD` | 维护者push后要求local/remote/Cloud `git rev-parse HEAD`三者相等 |
| package identity | `package.json`与Release artifact均为`0.4.2` | 模板4.1与builder check |
| installed predecessor | exact `0.4.1`、manifest schema 3、12-file installed inventory | transition contract、contracts/installer/publication tests |
| accepted/fallback | accepted `v0.4.1`；immediate fallback `v0.4.0` | ROADMAP与publication oracle；不由candidate测试改写 |
| candidate ZIP | `pwf-codex-cloud-hooks-v0.4.2.zip`，22项contract allowlist | 两次build/check、`cmp`、实际size/SHA输出 |
| external bootstrap | `init-cloud-sandbox-v0.4.2.bash`；Source/Candidate阶段64位zero hash | Bash syntax与zero-hash fail-closed测试；seal后才能写exact ZIP SHA |
| Cloud runtime | disposable Linux Cloud；Node major由维护者先在独立fact-only task确认并写入`PWF_ACCEPTANCE_NODE_MAJOR` | 正式4.1 task重新核对major |

任何worktree、HEAD、contract/hash、inventory或环境漂移都立即停止。Published Release所需URL、filename、size与SHA只有
immutable publication后才能写入本guide的channel checkpoint；当前不得预填。

## 3. 执行教程

### 3.1 本地与维护者push preflight

智能体完成完整本地回归、Release双构建和本地commit后，向维护者交接`SOURCE_CANDIDATE_HEAD`。维护者在本地执行：

```powershell
git status --short --branch
$expectedHead = (git rev-parse HEAD).Trim()
if (git status --porcelain) { throw "v0.4.2 Source/Candidate blocked: worktree is not clean" }
git push -u origin 0.4.2
$remoteLine = git ls-remote origin refs/heads/0.4.2
if (-not $remoteLine) { throw "v0.4.2 remote branch was not found" }
$remoteHead = ($remoteLine -split '\s+')[0]
if ($remoteHead -ne $expectedHead) { throw "v0.4.2 HEAD mismatch: local=$expectedHead remote=$remoteHead" }
"SOURCE_CANDIDATE_HEAD=$expectedHead"
```

远端branch写入由维护者负责。此时不得创建tag、Pre-release、上传资产或把zero-hash bootstrap当作公开安装入口。

### 3.2 Source/Candidate Cloud

在全新disposable Cloud environment选择已push的exact `0.4.2` checkout。每个新task都先附
[`Cloud task验收权限前缀`](../cloud-hard-acceptance-template.md#cloud-task-acceptance-permission-prefix)，再按下表执行；
不得复制旧聊天或历史acceptance中的脚本。

| 顺序 | 操作 | 唯一协议来源 | 必须回传 |
|---|---|---|---|
| A | 新setup task执行4.1 | [`Source/Candidate setup`](../cloud-hard-acceptance-template.md#source-candidate-setup) | 最终exit code、HEAD、portable Linux TAP、双ZIP identity、install/doctor与`PWF_SOURCE_CANDIDATE_SETUP=PASS` |
| B | A PASS后新task执行5.1 | [`post-install Resume`](../cloud-hard-acceptance-template.md#blackbox-post-install-resume) | 原始回复与真实SessionStart source |
| C | B同task执行第6节 | [`canonical baseline`](../cloud-hard-acceptance-template.md#blackbox-canonical-baseline) | exact plan ID与acknowledgment；唯一允许的workspace改动 |
| D | 同task执行第7节 | [`canonical context`](../cloud-hard-acceptance-template.md#blackbox-canonical-context) | 八项原始观察 |
| E1/E2 | 同task执行8.1后，从UI Resume同一task执行8.2 | [`real Resume`](../cloud-hard-acceptance-template.md#blackbox-real-resume) | acknowledgment、catch-up、tail、真实Resume source与顺序 |
| F | E2同一resumed task重新提取当前模板9.1 | [`Source/Candidate deep check`](../cloud-hard-acceptance-template.md#source-candidate-deep-check) | 最终exit code、HEAD、doctor、inventory、policy、residue与`PWF_SC_POST_RESUME=PASS` |

A/F HEAD必须同时等于维护者交接的`SOURCE_CANDIDATE_HEAD`。C产生的`.planning` fixture不提交，通道完成后丢弃环境。

### 3.3 Source/Candidate通过后的停止点

维护者把完整原始证据交回后，智能体先核对Source/Candidate真实PASS，再执行source-candidate closeout retirement checkpoint。
这次第一轮真实审查只对Release-excluded planning、临时教程和脚手架提出`RETIRE/MIGRATE/KEEP`；planning删除仍需维护者按
[Planning生命周期](../repository-governance-guide.md#planning-lifecycle)明确决定。随后把Source/Candidate channel checkpoint与
第一退役检查的真实结论一起写回，创建C1 `SOURCE_CANDIDATE_CHECKPOINT_HEAD`。正式`v0.4.2` tag必须继续精确指向C0
`SOURCE_CANDIDATE_HEAD`，不能指向C1。

若拟清退对象会改变package、contract、runtime、bootstrap、ZIP allowlist或其他C0 Release输入，立即停止：不得沿用这次PASS，
必须形成新C0并重新运行Source/Candidate。大白话：验收通过后只能清理不进入Release字节的施工材料；碰到C0输入就要重新验收。

C1 push后才由维护者创建immutable Pre-release并上传最终ZIP与ZIP外bootstrap。公开资产形成后，本guide再追加exact public
identity，独立Fresh Cloud按模板[`Published Release setup`](../cloud-hard-acceptance-template.md#published-release-setup)、
5.2、C～E与[`Published Release deep check`](../cloud-hard-acceptance-template.md#published-release-deep-check)执行第二通道。

<a name="v0-4-2-published-release-cloud"></a>

### 3.4 Published Release Cloud

本通道使用另一套Fresh Cloud，只验证公开默认下载链；不得设置`HOOKS_URL`、`HOOKS_SHA256`或任何ZIP override。版本化输入冻结为：

| 公开输入 | Exact identity |
|---|---|
| Release/tag | `v0.4.2`；tag source `d51f291566b5599cb21a9fc5c3f30fd1a1bbc74a` |
| bootstrap | `https://github.com/keeptoy/pwf-codex-cloud-hooks-next/releases/download/v0.4.2/init-cloud-sandbox-v0.4.2.bash`；SHA-256 `4c04b4758bce0f3e9eb22afcba05dcb8788958357c24e31014a55edf850dec64` |
| ZIP | `https://github.com/keeptoy/pwf-codex-cloud-hooks-next/releases/download/v0.4.2/pwf-codex-cloud-hooks-v0.4.2.zip`；SHA-256 `d1547ab50afcc3a275592d41b60daa77ab1062c97c072be3661cfe763467264e` |

每个新task仍先附[`Cloud task验收权限前缀`](../cloud-hard-acceptance-template.md#cloud-task-acceptance-permission-prefix)，再按下表执行。
表格只编排版本identity和稳定anchors，不复制模板脚本或提示词：

| 顺序 | 操作 | 唯一协议来源 | 必须回传 |
|---|---|---|---|
| A | Fresh environment setup执行4.2，只替换上表bootstrap URL/SHA | [`Published Release setup`](../cloud-hard-acceptance-template.md#published-release-setup) | bootstrap checksum、默认下载安装结果与`PWF_PUBLIC_RELEASE_SETUP=PASS` |
| B | setup后的首个新task执行5.2 | [`Fresh startup`](../cloud-hard-acceptance-template.md#blackbox-fresh-startup) | 原始回复、SessionStart/UserPromptSubmit与真实startup source |
| C | B同task执行第6节 | [`canonical baseline`](../cloud-hard-acceptance-template.md#blackbox-canonical-baseline) | exact plan ID与acknowledgment；唯一允许的workspace改动 |
| D | 同task执行第7节 | [`canonical context`](../cloud-hard-acceptance-template.md#blackbox-canonical-context) | 八项原始观察 |
| E1/E2 | 同task执行8.1后，从UI Resume同一task执行8.2 | [`real Resume`](../cloud-hard-acceptance-template.md#blackbox-real-resume) | acknowledgment、catch-up、tail、真实Resume source与顺序 |
| F | E2同一resumed task重新取得当前模板9.2，只替换上表ZIP URL/SHA | [`Published Release deep check`](../cloud-hard-acceptance-template.md#published-release-deep-check) | 最终exit code、公开ZIP重下载、doctor、inventory、policy、residue与`PWF_PUBLIC_POST_RESUME=PASS` |

C产生的`.planning` fixture不得提交，通道完成后随disposable环境退出。任何公开URL、size、SHA、tag source、default download、
Fresh/Resume或最终exit code不匹配都立即停止；不得重新打包、重传同名资产或移动tag修补。

## 4. 证据与停止条件

共同硬停止以[`模板第2节`](../cloud-hard-acceptance-template.md#acceptance-hard-stops)为准，并增加：

- production、runtime bundle、Host ABI、managed events或adapter-only policy相对v0.4.1发生非预期变化；
- transition不是exact v0.4.1 predecessor，或候选宣称支持未列入contract的旧installed state；
- CHANGELOG/ROADMAP/guide把未运行通道写成PASS，或provenance提前出现未发布v0.4.2身份；
- Source/Candidate开始前bootstrap已经含非zero默认ZIP SHA，或publication后重新打包却沿用原tag/资产身份；
- Cloud模型自动修改候选、commit、push、创建PR/Release或没有取得真实最终exit code。

维护者回传必须保存A/F完整stdout/stderr和最终exit code、portable suite摘要、ZIP entries/size/SHA、B～E原始回复、
doctor JSON、inventory/policy/residue，以及任何首次失败输出。失败后若环境或候选字节改变，必须Fresh重跑对应通道。

## 5. Pre-run 与 channel checkpoint

### 5.1 Candidate admission preflight — 2026-08-23

| 对象 | 只读盘点 | 当前理由与下一review条件 |
|---|---|---|
| 本Release closeout活动planning | `PRESENT / KEEP` | 承担当前授权、C0/C1输入和失败恢复；第一轮与第二轮都只提醒维护者评审，未经明确决定不删除 |
| 19个v0.4.2 documentation-governance completed scopes | `PRESENT / KEEP` | 当前列车的实施/治理恢复证据；保留到第二轮role-window closeout复核，不自动删除 |
| 5个更早v0.4.0/v0.4.1/source-analysis completed scopes | `PRESENT / FIRST-REVIEW CANDIDATES` | 已有immutable Git/history/acceptance恢复；只在Source/Candidate PASS后进入第一轮真实评审，删除仍需维护者明确决定 |
| v0.4.1 P9-F测试证据 | `PREREQUISITE MIGRATION VERIFIED` | current test在C0物化前已改为只读immutable v0.4.1 acceptance；preflight只核对结果，没有删除旧planning |
| v0.4.1 bootstrap与acceptance | `PRESENT / KEEP` | 当前accepted运维入口和immutable证据；v0.4.2晋级后在第二检查点轮转 |
| v0.4.2 package/contract/bootstrap/本guide | `C0 INPUT / KEEP` | candidate + accepted角色窗口的当前输入，第一轮不得改动后沿用原PASS |

Preflight结论：所有Release输入变更均纳入本次C0重新冻结；这里只完成inventory、分类、恢复证据和风险检查，没有执行planning、
恢复材料、回滚线索或其他对象删除，也没有新增Cloud通道。Source/Candidate失败时这些材料继续保留；真实第一轮只在PASS后执行并
写入C1，真实第二轮只在Published Release PASS、GitHub Release Latest promotion confirmation后执行并写入C2。

当前状态：

`V0_4_2_PRE_RUN_READY / SOURCE_CANDIDATE_NOT_RUN / STOP_BEFORE_MAINTAINER_PUSH`

<a name="v0-4-2-source-candidate-channel-checkpoint"></a>

### 5.2 Source/Candidate channel checkpoint — PASS

维护者确认C0 `d51f291566b5599cb21a9fc5c3f30fd1a1bbc74a`已在独立Cloud完整执行本guide 3.2的A～F，
整条Source/Candidate结论为`PASS`。A～E的完整原始输出继续由维护者保存；本节不凭摘要重造未回传的plan ID、逐步回复或测试数字。
最终9.1原始结果明确给出：

```text
PWF_WORKTREE_CHANGES=PLANNING_ONLY
POST_RESUME_DOCTOR=PASS
PWF_DEEP_CHECK_MANIFEST_SCHEMA=4
PWF_DEEP_CHECK_RELEASE_CONTRACT_PATH=contracts/release-artifact-v2.json
PWF_DEEP_CHECK_RELEASE_CONTRACT_ID=PWF_RELEASE_ARTIFACT_V2
PWF_DEEP_CHECK_RELEASE_SCHEMA=2
PWF_DEEP_CHECK_BUNDLE_CONTRACT_PATH=contracts/runtime-bundle-v2.json
PWF_DEEP_CHECK_BUNDLE_CONTRACT_ID=PWF_MANAGED_RUNTIME_BUNDLE_V2
PWF_DEEP_CHECK_BUNDLE_SCHEMA=2
PWF_DEEP_CHECK_INSTALLED_ROOT=hooks/planning-with-files
INSTALLER_VERSION=0.4.2
RELEASE_ARTIFACT_ENTRIES=22
INSTALLED_RUNTIME_FILES=12
UPSTREAM_PRISTINE_FILES=4
BUNDLE_INSTALLED_INVENTORY=AUTHORITATIVE
MANAGED_POLICY=ADAPTER_ONLY
PWF_DEEP_CHECK_HEAD=d51f291566b5599cb21a9fc5c3f30fd1a1bbc74a
SNAPSHOT_LEFTOVERS=0
PWF_SC_POST_RESUME=PASS
PWF_9_1_EXTRACT_EXIT=0
PWF_9_1_SCRIPT_EXIT=0
```

Doctor同时为`healthy=true`、`repairable=false`、`managed=true`，events精确为`SessionStart`与
`UserPromptSubmit`，`errors=[]`、`blockers=[]`。4.1 setup的最终通道标记为`PWF_SOURCE_CANDIDATE_SETUP=PASS`。

C步骤首次安全停止值得保留为诊断时间线：B的限制已经结束，但旧C协议自己重新规定“不得运行Shell”，而该Cloud会话只有
Shell型读取和apply_patch，没有独立只读文件工具。模型没有在无法核对“存在即停止”条件时盲目Add File；维护者随后临时授权
只读Shell existence preflight，正文仍只用apply_patch创建，C及后续D～F顺利PASS。该事实确认问题属于教程能力声明，不是
runtime/product缺陷；通用模板现已吸收同一有界预检，并继续禁止Shell写入、覆盖、commit、push或PR。

source-candidate closeout retirement checkpoint结果：

| 对象 | 决定 | 理由 / 下一检查点 |
|---|---|---|
| 本Release closeout活动planning | `KEEP` | 继续承担C1、publication、Published Release与C2恢复；删除仍需维护者明确决定 |
| 所有仓库内planning与历史恢复账 | `KEEP` | 本轮未取得逐项删除授权；已有Git恢复点或完成状态都不自动授权清退，C2再次复核 |
| Cloud canonical fixture | `RETIRE WITH DISPOSABLE ENVIRONMENT` | 只属于本轮Cloud黑盒且不得提交；随验收环境退出，不形成仓库删除事务 |
| C步骤临时Shell授权 | `MIGRATE TO TEMPLATE CONTRACT` | 已收窄为无独立只读工具时的exact-path只读preflight；apply_patch仍是唯一写入工具 |
| v0.4.1 accepted guide、三份template与v0.4.2 current guide | `KEEP` | 分别承担accepted回滚、稳定协议和Published Release后续职责；角色退出点仍在C2 |
| package、contract、runtime、bootstrap、ZIP allowlist与其他C0输入 | `KEEP / BYTE-UNCHANGED` | 本次C1只改Release-excluded文档、tests和planning，不触发新C0或重跑第一通道 |

第一轮没有删除任何仓库对象。C1只保存已经发生的Source/Candidate与retirement结论；它不替代C0。维护者push本次状态commit后，
正式`v0.4.2`验收tag仍必须精确指向`d51f291566b5599cb21a9fc5c3f30fd1a1bbc74a`，然后才能创建immutable Pre-release并
上传已验收ZIP与ZIP外bootstrap。

当前状态：

`SOURCE_CANDIDATE_PASS / PUBLISHED_RELEASE_NOT_RUN / STOP_BEFORE_PUBLICATION`

<a name="v0-4-2-published-release-channel-checkpoint"></a>

### 5.3 Published Release channel checkpoint — PASS

本次执行发生在3.4操作表补齐之前；维护者直接从当时current
[`Cloud hard acceptance template`](../cloud-hard-acceptance-template.md#published-release-setup)取得4.2、5.2、6、7、8和9.2，
在独立Fresh Cloud完成整条通道并确认`PASS`。这不降低证据等级：稳定模板本来就是唯一执行协议；本次补表只是修复版本guide的
可发现性，未改写脚本、运行输入或结果。

公开publication identity经维护者回传与只读GitHub审计共同确认：

| Identity | Exact evidence |
|---|---|
| Release | `https://github.com/keeptoy/pwf-codex-cloud-hooks-next/releases/tag/v0.4.2`；`draft=false`、`prerelease=true` |
| tag source | `d51f291566b5599cb21a9fc5c3f30fd1a1bbc74a`，精确等于Source/Candidate C0 |
| ZIP | `pwf-codex-cloud-hooks-v0.4.2.zip`；22 entries；87,386 bytes；SHA-256 `d1547ab50afcc3a275592d41b60daa77ab1062c97c072be3661cfe763467264e` |
| bootstrap | `init-cloud-sandbox-v0.4.2.bash`；21,565 bytes；SHA-256 `4c04b4758bce0f3e9eb22afcba05dcb8788958357c24e31014a55edf850dec64` |

9.2最终exit code为0，关键原始输出为：

```text
release.zip: OK
PUBLIC_PACKAGE_IDENTITY=0.4.2
POST_RESUME_DOCTOR=PASS
PWF_DEEP_CHECK_MANIFEST_SCHEMA=4
PWF_DEEP_CHECK_RELEASE_CONTRACT_PATH=contracts/release-artifact-v2.json
PWF_DEEP_CHECK_RELEASE_CONTRACT_ID=PWF_RELEASE_ARTIFACT_V2
PWF_DEEP_CHECK_RELEASE_SCHEMA=2
PWF_DEEP_CHECK_BUNDLE_CONTRACT_PATH=contracts/runtime-bundle-v2.json
PWF_DEEP_CHECK_BUNDLE_CONTRACT_ID=PWF_MANAGED_RUNTIME_BUNDLE_V2
PWF_DEEP_CHECK_BUNDLE_SCHEMA=2
PWF_DEEP_CHECK_INSTALLED_ROOT=hooks/planning-with-files
RELEASE_ARTIFACT_ENTRIES=22
INSTALLED_RUNTIME_FILES=12
UPSTREAM_PRISTINE_FILES=4
BUNDLE_INSTALLED_INVENTORY=AUTHORITATIVE
MANAGED_POLICY=ADAPTER_ONLY
PWF_PUBLIC_ZIP_REDOWNLOAD_SHA256=d1547ab50afcc3a275592d41b60daa77ab1062c97c072be3661cfe763467264e
PWF_PUBLIC_ZIP_BOUNDARY_IMPORTER=PASS
SNAPSHOT_LEFTOVERS=0
PWF_PUBLIC_POST_RESUME=PASS
PWF_9_2_EXIT_CODE=0
```

Doctor为`healthy=true`、`repairable=false`、`managed=true`，events精确为`SessionStart`与`UserPromptSubmit`，
`errors=[]`、`blockers=[]`。Cloud canonical planning的Phase 2已标记complete；Cloud内commit `0dc2e3b`只保存disposable
fixture结果，不是本仓库C2、tag source或programme identity。

Published Release PASS只允许进入维护者的Latest promotion，不自动晋级。本checkpoint形成时Release仍是Pre-release，
ROADMAP accepted继续为v0.4.1；后续实际promotion与流程校准见5.4，第二轮role-window closeout和C2 final Post-run仍等待后续真实证据。

当前状态：

`SOURCE_CANDIDATE_PASS / PUBLISHED_RELEASE_PASS / LATEST_NOT_RUN / STOP_BEFORE_LATEST`

<a name="v0-4-2-latest-promotion-confirmation"></a>

### 5.4 GitHub Release Latest promotion confirmation — PASS

维护者在GitHub Release编辑页面把同一个`v0.4.2` Release取消Pre-release并设为Latest；保存后返回或刷新GitHub Release详情页，
确认该exact版本显示为Latest且不再是Pre-release。2026-08-24公开状态核对为：

```text
latest_tag=v0.4.2
draft=false
prerelease=false
tag_source=d51f291566b5599cb21a9fc5c3f30fd1a1bbc74a
zip_size=87386
zip_sha256=d1547ab50afcc3a275592d41b60daa77ab1062c97c072be3661cfe763467264e
bootstrap_size=21565
bootstrap_sha256=4c04b4758bce0f3e9eb22afcba05dcb8788958357c24e31014a55edf850dec64
```

本节只保存本次GitHub Release状态变更、公开核对和exact identity的真实证据，不重新定义通用成功条件、是否另设独立
postflight或异常诊断边界；这些规则只读
[`ROADMAP`的GitHub Release Latest promotion confirmation](../../ROADMAP.md#github-release-latest-promotion-confirmation)。
依据该authority，上述本次证据记为`PASS`。

该PASS只允许进入第二轮role-window closeout。programme accepted仍暂记v0.4.1，直到C2把第二退役结论与角色轮转原子写回；
本节没有授权自动删除planning、v0.4.1 current guide、bootstrap或其他窗口对象。

当前状态：

`SOURCE_CANDIDATE_PASS / PUBLISHED_RELEASE_PASS / LATEST_PROMOTION_CONFIRMED / STOP_BEFORE_ROLE_WINDOW_CLOSEOUT`

<a name="v0-4-2-role-window-closeout"></a>

### 5.5 Role-window closeout retirement checkpoint / final Post-run — PASS

维护者已明确授权本轮具体清退对象。执行前逐项审计确认24个非活动planning scope都具备`task_plan.md`、`findings.md`、
`progress.md`，共72个文件全部被Git追踪、没有未提交改动、没有current外部入链；其中23个scope没有未勾选项。唯一例外是旧
Windows path-safety账本仍保留一条Linux/POSIX证据复选框，但该证据已由后继Linux gate实际完成并由Phase 4.13固化，旧账本自身也
明确标记`PLAN_CLOSED`，因此不是未完成工作。

第二轮真实决定如下：

| 对象 | 决定 | 恢复/保留理由 |
|---|---|---|
| 24个非活动planning scope | `RETIRE` | 已完成、干净、无current入链；72个文件继续由Git历史恢复 |
| 本Release closeout活动planning | `KEEP` | 保存C2本地commit与维护者push交接；不在本次删除范围 |
| v0.4.1 current guide与bootstrap | `RETIRE` | 已退出accepted current窗口；guide由exact Git commit `3903326d7bbea344a8b03de1d9e1e7205eed57b1`恢复，bootstrap由immutable tag/Release恢复 |
| publication oracle | `MIGRATE` | 继续动态读取ROADMAP角色，验证`v0.4.2` accepted与`v0.4.1` immediate fallback的immutable package与恢复路径 |
| v0.4.2 guide/bootstrap | `KEEP / FREEZE` | 当前accepted入口与公开资产身份；不得随C2改写sealed字节 |
| contracts、runtime、templates、history与production tests | `KEEP` | 长期协议、行为与历史理由，不因角色轮转机械删除 |
| 远端v0.4.0/v0.4.1/v0.4.2 tag、Release与资产 | `KEEP / IMMUTABLE` | 本轮只清退current-tree副本；所有已发布身份保持原字节和URL |

本地`v0.4.2` tag已核对并精确指向已验收C0
`d51f291566b5599cb21a9fc5c3f30fd1a1bbc74a`。programme角色原子轮转为：`v0.4.2` accepted、`v0.4.1`
immediate fallback、`v0.4.0` deeper fallback。下一Product Phase和下一版本列车均未授权。

最终状态：

`SOURCE_CANDIDATE_PASS / PUBLISHED_RELEASE_PASS / LATEST_PROMOTION_CONFIRMED / ROLE_WINDOW_CLOSEOUT_PASS / C2_COMPLETE / NEXT_TRAIN_UNAUTHORIZED`
