# v0.3.1 Cloud hard acceptance

> 目的：让第一次接手本仓库的维护者，可以在一个全新的 Codex Cloud 沙箱中，重放
> 已发布并完成最终验收的 `0.3.1` 安全修复 Release 的 Linux、setup、Fresh lifecycle、canonical planning、真实 Resume
> catch-up、doctor、inventory 与 residue 验证。
>
> 当前状态：Attempt 2 的 exact source/ZIP 已完成 `S2 PASS`；其后的 README-only Release-input delta
> 没有冒充一次新的 Cloud PASS。`S3-A LOCAL SEAL PASS`、`S3-B PUBLICATION PASS` 与 S3-C 本地
> public-byte verification PASS；维护者执行的 S3-C final Cloud smoke 及 B～F 已全部 PASS，详见第 14 节。
> 本文不授权修改/重发 Release。S3-C 关闭后，维护者已另行授权并完成 rollback/Latest promotion：
> accepted production rollback 与 `Latest` 现为不可变 `v0.3.1`；v0.3.0 是 immediate previous
> fallback，beta.2 是更早的不可变 fallback oracle。详见第 15 节。

## 0. 新人快速路线

以下路线是已经通过的 S3-C 重放流程；再次执行仍须由届时活动 task plan 明确授权。S3-C 的 PASS
本身不产生 rollback/Latest promotion 授权。

1. 在 Codex Cloud 环境中选择 Node 22。
2. 选择分支 `validation/v0.3.1-s2-runbook`，创建全新容器或先 Reset cache，不复用生产环境。
3. **当前只把第 13 节脚本作为 setup script**；确认结尾出现
   `V031_S3C_PUBLIC_RELEASE_SETUP=PASS`。第 3 节属于已经关闭的 Source/Candidate S2 通道，当前不得执行。
4. setup 完成后创建一个完全新的 task，依次原样发送第 4～8 节的 B、C、D、E1、E2、F 提示词。
5. 保存原始输出和第 13 节末尾的 S3-C 摘要，不要只保存人工总结。
6. 任一步失败立即停止，不 repair 掩盖失败，也不自行进入 rollback/Latest promotion。

setup 中的 direct adapter probe 只证明安装后的静态调用链可执行，不是 Runtime 自动注入的黑盒
证据。B 必须在 setup 结束后创建的全新 task 中验证。

### 两条 Cloud 验收通道不得混用

| 通道 | 身份与用途 | 当前状态 |
|---|---|---|
| Source/Candidate | 选择开发/验证分支，但把 PASS 绑定到运行时记录的精确 commit；从源码 build/check ZIP，并用显式本地 `HOOKS_URL`/`HOOKS_SHA256` override 安装 | v0.3.1 S2 已关闭；第 3 节不是当前入口，第 4～8 节的固定黑盒 fixture 由 S3-C 复用 |
| Published Release | 固定 immutable tag/source，从公开 Release URL 下载并校验 bootstrap/ZIP，让公开 bootstrap 使用自身默认 URL 安装 | v0.3.1 S3-C 已关闭；重放时从第 13 节进入，再执行第 4～8 节 |

分支只是运输入口，不是验收身份；每次 Source/Candidate PASS 只属于当次输出的完整 commit SHA。Published
Release PASS 只属于精确 tag、公开资产字节和校验和，不能由可移动分支里的同名 bootstrap 代替。尤其
不得把旧远端分支中的 64 位 zero-hash bootstrap 与新版 sealed-hash runbook 拼在同一次执行中。

## 1. 当前验收身份

| 项目 | 当前/历史值 |
|---|---|
| package version | `0.3.1` |
| validation ref | `validation/v0.3.1-s2-runbook` |
| validation source | 每次运行时记录该分支当前完整 HEAD，不在本文写死 |
| published tag/source | `v0.3.1` / `9aa2148886e499f9f45594f7ae4f7681f1045de2` |
| Release | `https://github.com/keeptoy/pwf-codex-cloud-hooks-next/releases/tag/v0.3.1` |
| current S3-A sealed ZIP entries / size | 23 / 82,725 bytes |
| current S3-A sealed ZIP SHA-256 | `f097b04015b1a3847ca5a24b9236f882c5a008b22033793b5661e282c39131f9` |
| last exact Cloud-PASS source | `5b619dafebea25a82fdd9dfe3f4da185c644f58d` |
| last exact Cloud-PASS ZIP | 23 / 82,421 bytes / `2cd19e04a15995014ae354ad0319e4182a72ea0fc82b08213959b3550c741cfb` |
| candidate bootstrap | `init-cloud-sandbox-v0.3.1.bash` |
| S3-A sealed bootstrap | 21,565 bytes / SHA-256 `ce31a32002aea46bbf3f9baf9a0e93451d24c3b3653952e425d1e1ff6960a5e8` |
| candidate bootstrap ZIP hash | 精确 `f097b040...31f9`；显式 zero override 仍 fail closed |
| S3-C local public-byte check | PASS；两个公开 URL 的下载 filename/size/SHA 与 extracted importer 均匹配 |
| S3-C final Cloud smoke | PASS；public setup 与 B～F 全部通过，F 原始结果见第 14 节 |
| rollback / GitHub Latest | `v0.3.1`；promotion 证据见第 15 节 |
| managed runtime payloads | 11，另有 `installed-manifest.json` |
| required Cloud Node | 22；产品支持边界仍为 Node `>=18` |

当前 validation ref 已完成最终 S3-C 操作说明的 Cloud 运输，应冻结为版本证据，不再充当
长期滚动开发分支。未来滚动源码验收使用另行授权的 `dev`，必要时再使用短期 feature/security 分支；
无论分支名称是什么，每次 Cloud 运行都必须从干净 checkout 开始并输出本次完整 HEAD。

第 3 节和第 9～12 节记录已经关闭的 Source/Candidate S2 通道及其历史尝试；第 4～8 节是 S3-C 明确
复用的固定黑盒 fixture；第 13 节是 Published Release setup 入口。publication、downloaded-byte
Cloud PASS 与后续独立 rollback promotion 均已发生。已完成的 Attempt 2 source/ZIP 证据
冻结在第 12 节，不能用当前值覆盖。任何发布字节问题都必须使用新身份，不能修改现有 tag/资产。

## 2. 硬停止条件

遇到任一情况立即停止并保存输出：

- checkout 不是 `validation/v0.3.1-s2-runbook` 的待验收版本，或工作树在 setup 入口不干净；
- Node major 不是 22；
- Linux suite 有任意 failure 或 skip；
- ZIP entry、size、hash、双构建或 extracted importer 不一致；
- 发现当前沙箱已有 `/opt/codex/hooks/planning-with-files`，说明不是 Fresh setup；
- bootstrap、doctor、Managed policy 或 pristine Skill 校验失败；
- B～F 任一步缺少必需 Runtime 自动注入证据；
- post-resume doctor 不健康、可 repair、存在 blocker/error、inventory 不一致或有 snapshot residue。

失败后不得修改候选、放宽断言、手工执行 adapter 伪造黑盒结果、repair 后继续计为同一次 PASS，或把
Windows skip 当作 Linux 证据。修复必须回到新的授权 gate，并重新开始一次 Fresh Cloud 验证。

## 3. Source/Candidate setup：Linux 回归与一次性候选安装（S2 已关闭）

> **历史/开发通道，当前 S3-C 禁止执行。** 本节保留 Source/Candidate 验收机制和已关闭 S2 的证据
> 结构；只有未来活动计划重新授权源码 gate 时才能使用，并且必须让 runbook、checkout 和 bootstrap
> 来自同一受审身份。当前 v0.3.1 最终验收直接跳到第 13 节。

获批时也只在全新、可丢弃的 Codex Cloud 环境运行。它会把候选安装到该容器的 `/opt/codex` 和
Managed requirements；这个容器只能用于对应源码验证，不得作为 production 或 rollback 环境复用。

```bash
set -Eeuo pipefail

readonly EXPECTED_ZIP_SHA256="f097b04015b1a3847ca5a24b9236f882c5a008b22033793b5661e282c39131f9"
readonly TARGET_CODEX_HOME="${CODEX_HOME:-/opt/codex}"

PROBE_DIR="$(mktemp -d)"
trap 'rm -rf -- "$PROBE_DIR"' EXIT

test -z "$(git status --short)"
readonly RUNBOOK_HEAD="$(git rev-parse HEAD)"
printf 'RUNBOOK_BRANCH=%s\n' "$(git branch --show-current)"
printf 'RUNBOOK_HEAD=%s\n' "$RUNBOOK_HEAD"
git status --short --branch
git diff --check

test "$(python3 -c 'import json; print(json.load(open("package.json", encoding="utf-8"))["version"])')" = "0.3.1"
test "$(node -p 'process.versions.node.split(".")[0]')" = "22"
grep -Fq 'HOOKS_SHA256:-f097b04015b1a3847ca5a24b9236f882c5a008b22033793b5661e282c39131f9' \
  init-cloud-sandbox-v0.3.1.bash

python3 tools/import_upstream_runtime.py check
python3 - <<'PY'
from pathlib import Path

for value in (
    "hooks/hook_adapter.py",
    "runtime/owned-plan.py",
    "runtime/owned-catchup.py",
):
    path = Path(value)
    compile(path.read_text(encoding="utf-8"), str(path), "exec")
print("PYTHON_STATIC=PASS")
PY
node --check install.js
bash -n init-cloud-sandbox-v0.3.1.bash
git diff --check

TEST_OUTPUT="$PROBE_DIR/tests.tap"
node --test --test-reporter=tap tests/*.test.js | tee "$TEST_OUTPUT"
grep -Eq '^# fail 0$' "$TEST_OUTPUT"
grep -Eq '^# skipped 0$' "$TEST_OUTPUT"
TESTS="$(awk '/^# tests / {print $3}' "$TEST_OUTPUT" | tail -1)"
PASSES="$(awk '/^# pass / {print $3}' "$TEST_OUTPUT" | tail -1)"
test -n "$TESTS"
test "$PASSES" = "$TESTS"

mapfile -t EXECUTABLES < <(git ls-files --stage runtime/upstream | awk '$1 == "100755" {print $4}')
test "${#EXECUTABLES[@]}" -eq 4
printf '%s\n' "${EXECUTABLES[@]}" | grep -Fxq 'runtime/upstream/inject-plan.sh'
printf '%s\n' "${EXECUTABLES[@]}" | grep -Fxq 'runtime/upstream/ledger-summary.sh'
printf '%s\n' "${EXECUTABLES[@]}" | grep -Fxq 'runtime/upstream/resolve-plan-dir.sh'
printf '%s\n' "${EXECUTABLES[@]}" | grep -Fxq 'runtime/upstream/session-catchup.py'

ZIP_A="$PROBE_DIR/candidate-a.zip"
ZIP_B="$PROBE_DIR/candidate-b.zip"
python3 tools/build_release.py build --output "$ZIP_A"
python3 tools/build_release.py check --archive "$ZIP_A"
python3 tools/build_release.py build --output "$ZIP_B"
python3 tools/build_release.py check --archive "$ZIP_B"
cmp "$ZIP_A" "$ZIP_B"
test "$(unzip -Z1 "$ZIP_A" | wc -l)" -eq 23
test "$(wc -c < "$ZIP_A")" -eq 82725
ACTUAL_ZIP_SHA256="$(sha256sum "$ZIP_A" | awk '{print $1}')"
test "$ACTUAL_ZIP_SHA256" = "$EXPECTED_ZIP_SHA256"
unzip -Z1 "$ZIP_A" | grep -Fxq 'pwf-codex-cloud-hooks/patches/patch_planning_skill.py'
if unzip -Z1 "$ZIP_A" | grep -Fq 'init-cloud-sandbox-'; then
  printf 'Bootstrap unexpectedly entered candidate ZIP\n' >&2
  exit 1
fi

unzip -q "$ZIP_A" -d "$PROBE_DIR/extracted"
python3 "$PROBE_DIR/extracted/pwf-codex-cloud-hooks/tools/import_upstream_runtime.py" check

if [ -e "$TARGET_CODEX_HOME/hooks/planning-with-files" ]; then
  printf 'Fresh setup required; managed runtime already exists: %s\n' "$TARGET_CODEX_HOME" >&2
  exit 1
fi

HOOKS_URL="file://$ZIP_A" \
HOOKS_SHA256="$ACTUAL_ZIP_SHA256" \
bash init-cloud-sandbox-v0.3.1.bash all

test -z "$(git status --short)"
printf 'RUNBOOK_HEAD=%s\n' "$RUNBOOK_HEAD"
printf 'LINUX_SUITE=PASS tests=%s pass=%s fail=0 skipped=0\n' "$TESTS" "$PASSES"
printf 'ZIP_ENTRIES=23\n'
printf 'ZIP_SIZE=82725\n'
printf 'ZIP_SHA256=%s\n' "$ACTUAL_ZIP_SHA256"
printf 'BOOTSTRAP_DEFAULT_HASH=F097B040_SEALED\n'
printf 'V031_S2_DISPOSABLE_SETUP=PASS\n'
```

期望的 Linux-only 覆盖至少包括：

- root/root 与 synthetic install-user/Hook-user 两条真实 owned-runtime activation；
- installed runtime cross-user readable；
- owned plan private snapshot、resolver/attachment 和 unsafe-file 拒绝；
- replacement、truncate、append、hard-link race；
- injector/runtime process-group timeout 与 bounded cleanup；
- transcript symlink/hardlink 拒绝；
- installer transaction/concurrency 与 Managed TOML ownership preservation。

不要把历史 v0.3.0 文档中的固定测试数量复制到本次证据；数量从本次 runner 输出取得，但必须
`fail=0` 且 `skipped=0`。

## 4. B：Fresh lifecycle

setup 完成后创建完全新的 task。第一条消息原样发送：

```text
这是 pwf-codex-cloud-hooks v0.3.1 S2 Fresh lifecycle 黑盒验证。

严格限制：
1. 不要调用任何工具；
2. 不要运行 Shell；
3. 不要读取任何文件；
4. 不要自行执行 hook_adapter.py、owned-plan.py 或 owned-catchup.py；
5. 不要根据本提示、AGENTS.md 或仓库中的验收文档构造结果；
6. 只报告生成本次回复之前 Runtime 已自动注入的内容。

请逐字列出实际看到的所有以 PWF_GLOBAL_HOOK_CANARY_V1 开头的完整行。

然后严格汇总：
SessionStart: OBSERVED 或 NOT_OBSERVED
SessionStart source: 实际值；未观察到则写 NONE
UserPromptSubmit: OBSERVED 或 NOT_OBSERVED
Planning context（辅助观察）: OBSERVED 或 NOT_OBSERVED
===BEGIN PLAN DATA===（辅助观察）: OBSERVED 或 NOT_OBSERVED
=== recent progress ===（辅助观察）: OBSERVED 或 NOT_OBSERVED

没有实际看到的项必须写 NOT_OBSERVED。不要使用工具补救验证。
```

B 的必需条件：

- `SessionStart: OBSERVED`，source 必须为 `startup`；
- `UserPromptSubmit: OBSERVED`；
- 两条完整 canary 必须来自生成回复前的 Runtime 自动注入。

runbook 分支已经包含活动 planning，所以辅助 planning 字段可能为 `OBSERVED`；它们不是 B 的独立
通过条件，但必须如实记录。

## 5. C：创建真实 planning baseline

在 B 的同一个 task 中原样发送：

```text
这是 v0.3.1 S2 planning baseline 创建步骤。

请使用 apply_patch：
1. 创建 .planning/pwf-v031-s2-03a6/task_plan.md，内容第一行必须是：
   # PWF_V031_S2_CANONICAL_03A6
2. 创建同目录 progress.md，内容必须包含：
   PWF v0.3.1 S2 lifecycle baseline created by apply_patch.
3. 创建同目录 findings.md，内容必须包含：
   v0.3.1 S2 acceptance fixture.
4. 把 .planning/.active_plan 设置为：
   pwf-v031-s2-03a6
5. 完成后只回复：
   PWF_V031_S2_BASELINE_CREATED
```

必须实际产生 structured planning update，并收到精确 acknowledgment
`PWF_V031_S2_BASELINE_CREATED`。

## 6. D：canonical UserPromptSubmit

C 完成后立即原样发送：

```text
这是 v0.3.1 S2 canonical Planning context 自动注入验证。

严格限制：不要调用工具，不要运行 Shell，不要读取文件，不要根据本提示、AGENTS.md 或仓库中的验收文档构造标记；只报告生成本次回复之前 Runtime 自动注入的内容。

请严格汇总：
UserPromptSubmit canary: OBSERVED 或 NOT_OBSERVED
PWF_V031_S2_CANONICAL_03A6: OBSERVED 或 NOT_OBSERVED
[planning-with-files] ACTIVE PLAN: OBSERVED 或 NOT_OBSERVED
===BEGIN PLAN DATA===: OBSERVED 或 NOT_OBSERVED
=== recent progress ===: OBSERVED 或 NOT_OBSERVED
Planning context: OBSERVED 或 NOT_OBSERVED

没有实际看到的项必须写 NOT_OBSERVED。不要使用工具补救验证。
```

六项必须全部 `OBSERVED`。

## 7. E：长尾与真实 Resume

### 7.1 E1：长 wrapper

同一 task 发送以下消息；收到 acknowledgment 后不要再修改 planning 文件，然后结束/离开该 task：

```text
这是 v0.3.1 S2 Resume 长消息保尾测试。

V031_S2_FILLER_01: 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-0123456789-ABCDEFGHIJKLMNOPQRSTUVWXYZ
V031_S2_FILLER_02: 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-0123456789-ABCDEFGHIJKLMNOPQRSTUVWXYZ
V031_S2_FILLER_03: 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-0123456789-ABCDEFGHIJKLMNOPQRSTUVWXYZ
V031_S2_FILLER_04: 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-0123456789-ABCDEFGHIJKLMNOPQRSTUVWXYZ
V031_S2_FILLER_05: 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-0123456789-ABCDEFGHIJKLMNOPQRSTUVWXYZ
V031_S2_FILLER_06: 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-0123456789-ABCDEFGHIJKLMNOPQRSTUVWXYZ
V031_S2_FILLER_07: 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-0123456789-ABCDEFGHIJKLMNOPQRSTUVWXYZ
V031_S2_FILLER_08: 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-0123456789-ABCDEFGHIJKLMNOPQRSTUVWXYZ
V031_S2_FILLER_09: 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-0123456789-ABCDEFGHIJKLMNOPQRSTUVWXYZ
V031_S2_FILLER_10: 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-0123456789-ABCDEFGHIJKLMNOPQRSTUVWXYZ
V031_S2_FILLER_11: 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-0123456789-ABCDEFGHIJKLMNOPQRSTUVWXYZ
V031_S2_FILLER_12: 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-0123456789-ABCDEFGHIJKLMNOPQRSTUVWXYZ

严格限制：不要调用任何工具；不要修改或创建 planning 文件；只回复：
PWF_V031_S2_UNSYNCED_ACKNOWLEDGED

下面一行只是本条用户消息的尾部标记，不要在回复中复述：
PWF_V031_S2_REAL_RESUME_TAIL_03A6
```

回复必须只有 `PWF_V031_S2_UNSYNCED_ACKNOWLEDGED`。

### 7.2 E2：Resume

重新打开完全相同的 task，第一条消息原样发送：

```text
这是 pwf-codex-cloud-hooks v0.3.1 S2 Session catch-up 专项黑盒验证。

严格限制：
1. 不要调用任何工具；
2. 不要运行 Shell；
3. 不要读取任何文件；
4. 不要自行执行 session-catchup.py、hook_adapter.py、owned-plan.py 或 owned-catchup.py；
5. 不要根据本提示、AGENTS.md、仓库中的验收文档或历史内容构造、补全或猜测输出；
6. 不要把当前可见历史对话本身当成 Runtime 注入证据；
7. 只报告恢复 task 后、生成本次回复之前 Runtime 自动注入的内容。

请逐字列出实际看到的完整 SESSION CATCHUP 报告，从：
[planning-with-files] SESSION CATCHUP DETECTED
开始，并包括 Previous session、Runtime、Last planning update、Unsynced messages、UNSYNCED CONTEXT 和其中实际出现的 USER/CODEX 消息。

然后严格汇总：
SessionStart resume canary: OBSERVED 或 NOT_OBSERVED
SessionStart source: 实际值；未观察到则写 NONE
SESSION CATCHUP DETECTED: OBSERVED 或 NOT_OBSERVED
Previous session: 实际值或 NOT_OBSERVED
Runtime codex: OBSERVED 或 NOT_OBSERVED
Last planning update: 实际值或 NOT_OBSERVED
Unsynced messages: 实际数字或 NOT_OBSERVED
长消息截断标记出现在 UNSYNCED CONTEXT: OBSERVED 或 NOT_OBSERVED
PWF_V031_S2_REAL_RESUME_TAIL_03A6 出现在 UNSYNCED CONTEXT: OBSERVED 或 NOT_OBSERVED
Catch-up 位于 planning context 之前: OBSERVED 或 NOT_OBSERVED
PWF_V031_S2_CANONICAL_03A6: OBSERVED 或 NOT_OBSERVED
===BEGIN PLAN DATA===: OBSERVED 或 NOT_OBSERVED
Planning context: OBSERVED 或 NOT_OBSERVED

没有实际看到的项必须写 NOT_OBSERVED。不要使用工具补救验证。
```

所有汇总项必须成功观察；`Unsynced messages` 的具体数字不预设。`SessionStart source` 必须是
真实 Resume source，不能写 `startup`。

## 8. F：post-resume doctor、inventory、policy 与 residue

E2 PASS 后才运行：

```bash
set -Eeuo pipefail

PACKAGE_ROOT="$(git rev-parse --show-toplevel)"
DOCTOR_JSON="$(node "$PACKAGE_ROOT/install.js" doctor --json \
  --codex-home /opt/codex \
  --skill-root "$HOME/.agents/skills/planning-with-files" \
  --managed-requirements /etc/codex/requirements.toml)"
printf '%s\n' "$DOCTOR_JSON"

python3 - "$DOCTOR_JSON" <<'PY'
import json
import pathlib
import sys
import tomllib

doctor = json.loads(sys.argv[1])
assert doctor["healthy"] is True
assert doctor["repairable"] is False
assert doctor["errors"] == []
assert doctor["blockers"] == []

runtime = pathlib.Path("/opt/codex/hooks/planning-with-files")
manifest = json.loads((runtime / "installed-manifest.json").read_text(encoding="utf-8"))
expected = sorted(item["path"] for item in manifest["runtime_files"])
actual = sorted(
    str(path.relative_to(runtime)).replace("\\", "/")
    for path in runtime.rglob("*")
    if path.is_file() and path.name != "installed-manifest.json"
)
assert manifest["installer_version"] == "0.3.1"
assert expected == actual
assert len(actual) == 11

policy = tomllib.loads(pathlib.Path("/etc/codex/requirements.toml").read_text(encoding="utf-8"))
for event in ("SessionStart", "UserPromptSubmit"):
    groups = policy["hooks"][event]
    assert len(groups) == 1
    handlers = groups[0]["hooks"]
    assert len(handlers) == 1
    command = handlers[0]["command"]
    assert "hook_adapter.py" in command
    assert "owned-plan.py" not in command
    assert "owned-catchup.py" not in command

print("POST_RESUME_DOCTOR=PASS")
print("INSTALLER_VERSION=0.3.1")
print("INSTALLED_RUNTIME_FILES=11")
print("MANAGED_POLICY=ADAPTER_ONLY")
print("INSTALLED_RUNTIME_INVENTORY=" + json.dumps(actual, ensure_ascii=False))
PY

SNAPSHOT_BASE="${TMPDIR:-/tmp}/pwf-codex-cloud-hooks/snapshots"
LEFTOVERS=0
if [ -d "$SNAPSHOT_BASE" ]; then
  LEFTOVERS="$(find "$SNAPSHOT_BASE" -mindepth 1 -maxdepth 1 -type d -name 'pwf-snapshot-*' | wc -l)"
fi
test "$LEFTOVERS" -eq 0
printf 'SNAPSHOT_LEFTOVERS=0\n'
printf 'V031_S2_POST_RESUME=PASS\n'
```

## 9. 黑盒证据判定

若对当前 S3-A 本地封板源码重新执行一次 Cloud acceptance，必须同时具备：

- setup 干净 checkout、动态 HEAD 记录、Node 22、静态检查全部 PASS；
- Linux suite `fail=0`、`skipped=0`，并保存实际 tests/pass 数；
- 23-entry、82,725-byte、双构建一致的候选 ZIP 与 S3-A sealed hash
  `f097b04015b1a3847ca5a24b9236f882c5a008b22033793b5661e282c39131f9` 完全一致；
- extracted ZIP importer `check` PASS，patcher 在 ZIP 内，两个 bootstrap 均在 ZIP 外；
- disposable setup 使用重新下载的精确 ZIP；bootstrap 默认 hash 与该 ZIP 一致，显式 zero override 仍被拒绝；
- pristine PWF v3.8.2、adapter-only Managed policy、healthy doctor；
- B startup SessionStart 与 UserPromptSubmit canary；
- C/D 真实 planning update 与 canonical context；
- E 长消息尾标记、真实 Resume catch-up、顺序和恢复后的 planning context；
- F installer `0.3.1`、11 个精确 payload、adapter-only policy、零 snapshot residue；
- accepted v0.3.0 tag/bootstrap/资产没有被修改；
- 没有用提示词、仓库文本、手工脚本或当前可见历史伪造 Runtime 自动注入证据。

这套源码构建/setup 证据不能替代 S3-C：最终 acceptance 还必须在 S3-B publication 后，从 Release
页面重新下载 ZIP 与 bootstrap，先核对两者 filename/size/SHA，再以下载字节完成 Cloud smoke。

## 10. 本文明确不做的事

- 未获 S3-C 授权时，不下载、执行或验收 v0.3.1 Release 资产；
- 不创建或移动 tag；
- 不再次改写已完成 S3-A seal 的 candidate bootstrap hash；
- 不修改或重发 Release，不更新 `Latest`，不推送 `main`；
- 不把 S3-A 本地 seal 称为已发布、已下载验收或已接受 rollback；
- 不提升 v0.3.1 为 rollback；
- 不修改或重建 v0.3.0 的 tag、ZIP、bootstrap、SHA 或历史 acceptance；
- 不自行进入 S3-C 或 Product Phase 4。

历史 [v0.3.0 Cloud hard acceptance](https://github.com/keeptoy/pwf-codex-cloud-hooks-next/blob/1454c9224c83d11c073b05baf6e536a11c3bb0e5/docs/v0.3.0-cloud-hard-acceptance.md)
只作为已完成 stable 证据，不再是本次操作入口。

## 11. 回传证据模板

未来若重新执行当前候选，请把原始 console/task 输出与下面摘要一起交回维护者；不能用这份空模板
替代第 12 节已经冻结的 Attempt 2 证据：

```text
V031_S2_RUNBOOK_HEAD=<40-char commit>
V031_S2_NODE=<exact node version>
V031_S2_LINUX_SUITE=PASS tests=<actual> pass=<actual> fail=0 skipped=0
V031_S2_ZIP_ENTRIES=23
V031_S2_ZIP_SIZE=82725
V031_S2_ZIP_SHA256=f097b04015b1a3847ca5a24b9236f882c5a008b22033793b5661e282c39131f9
V031_S2_DISPOSABLE_SETUP=PASS
V031_S2_B_FRESH=PASS
V031_S2_C_BASELINE=PASS
V031_S2_D_CANONICAL=PASS
V031_S2_E1_TAIL_ACK=PASS
V031_S2_E2_REAL_RESUME=PASS unsynced_messages=<actual>
V031_S2_F_DOCTOR_INVENTORY_RESIDUE=PASS
V030_IMMUTABLE_ORACLE_UNCHANGED=PASS
V031_S2_CLOUD_HARD_ACCEPTANCE=PASS
V031_S3A_LOCAL_SEAL=PASS
S3_BC_AUTHORIZED=NO
```

填完本文只代表相应获批运行的 Cloud 证据；它不会自动提升 rollback。S3-A seal 与 S3-B publication
已经完成，仍必须另行授权 S3-C downloaded-asset/Cloud acceptance。

## 12. S2 执行记录

### Attempt 1 — STOPPED / test fixture

- Linux suite：79 registered / 78 passed / 1 failed / 0 skipped；
- failure：extracted importer 报告 `runtime mode mismatch for session_catchup`；
- 分类：候选 ZIP 的 builder metadata 已 PASS，失败来自测试夹具使用
  `zipfile.extractall()` 后没有应用 ZIP 中记录的 Unix executable mode；
- 处理：当前 runbook 分支已让测试解压器恢复 entry mode，严格 importer 校验保持不变；
- checkout 观察：stable commit object 存在但本地 `v0.3.0` tag ref 缺失。runbook 不再预检该
  tag；stable oracle 测试直接使用其固定 commit object，并只在 tag ref 存在时额外验证它；
- 因 `set -e` 在 suite failure 后停止，本轮未执行 ZIP byte-oracle、disposable install 或 B～F；
- production、Release inputs、bootstrap zero hash、remote tag/assets 和 rollback 均未变化。

下一次必须从全新 Cloud 环境重新执行第 3 节；Attempt 1 不能与后续结果拼接成 PASS。

### Attempt 2 — PASS / S2 closed

- validation source：rolling runbook HEAD
  `5b619dafebea25a82fdd9dfe3f4da185c644f58d`；
- Node：Cloud-selected major 22 precheck PASS；
- Linux suite：79 registered / 79 passed / 0 failed / 0 skipped；
- deterministic package：23 entries、82,421 bytes、双构建一致，SHA-256
  `2cd19e04a15995014ae354ad0319e4182a72ea0fc82b08213959b3550c741cfb`；
- extracted importer、disposable setup、pristine Skill、bootstrap zero-hash override 和 direct
  adapter probes PASS；
- 维护者确认 B Fresh、C baseline、D canonical、E1 tail acknowledgment 与 E2 real Resume 均按
  本文原始 Cloud 运行 PASS；未把 Attempt 1 与本轮拼接；
- F 精确输出：

```text
POST_RESUME_DOCTOR=PASS
INSTALLER_VERSION=0.3.1
INSTALLED_RUNTIME_FILES=11
MANAGED_POLICY=ADAPTER_ONLY
INSTALLED_RUNTIME_INVENTORY=["THIRD_PARTY_NOTICES.md", "compatibility-overlays-v1.json", "contracts/adapter-plan-context-request-v1.schema.json", "contracts/plan-context-result-v1.schema.json", "hook_adapter.py", "owned-catchup.py", "owned-plan.py", "upstream/inject-plan.sh", "upstream/ledger-summary.sh", "upstream/resolve-plan-dir.sh", "upstream/session-catchup.py"]
SNAPSHOT_LEFTOVERS=0
V031_S2_POST_RESUME=PASS
```

- 维护者最终判定：`V031_S2_CLOUD_HARD_ACCEPTANCE=PASS`；
- v0.3.0 rollback、beta.2 fallback、远端 tag/assets、bootstrap zero hash 与 Release inputs 未因
  Cloud 验收改变；
- S2 在此关闭。S3 seal、tag、publication、downloaded-asset revalidation 和 rollback promotion
  仍需分别授权。

### Post-S2 documentation-only delta — locally verified / Cloud not rerun

- README 的 patcher 说明先扩展、随后按维护者授权迁入 `ARCHITECTURE.md`；最终 README 只保留构建
  摘要和架构链接。四项 patcher 职责与两条路径现在由架构文档统一负责；
- `README.md` 是 23-entry ZIP 输入，因而候选 ZIP 字节发生变化；`ARCHITECTURE.md`、本文和
  `.planning/` 均在 Release ZIP 外，不影响候选字节；
- 最终本地双构建一致：23 entries、82,658 bytes，SHA-256
  `c527b555bbbffdc86796ff91753186c15bdab287852fc5667edb9e5493d952ff`；importer、Release
  self-containment、不可变 v0.3.0 oracle 与完整 Windows-local suite 均保持绿色；
- production code、runtime、installer、importer、patcher、contracts、bootstrap 和 trusted graph
  均未变化。维护者按风险比例决定不为普通文档 delta 单独重跑完整 S2；
- 当前 `c527...` 是供后续 seal 前复核的未封板本地观测值，不具有 Cloud PASS、seal、Release 或
  rollback 身份。Attempt 2 的 `2cd19e04...` 仍是最后一次 exact Cloud-PASS ZIP 证据；
- 该时点的下一关键边界是单独授权的 S3-A；它现已按下节完成。S3-C 仍必须对最终发布字节完成下载
  复核和 Cloud 冒烟。

### S3-A local immutable seal — PASS / local record

- 起点：维护者 checkpoint `9ef80422cb01c87240da884389f279c4910e2f2c`；
- 冻结 23 个 ZIP 输入；规范化输入清单 SHA-256
  `8b1fd450739e6ee90dc1b6210c3e9fe1831de36d03bb70a90438fc07124fdf58`；
- 独立双构建与 builder check 一致：23 entries、82,725 bytes、ZIP SHA-256
  `f097b04015b1a3847ca5a24b9236f882c5a008b22033793b5661e282c39131f9`；
- 外置 `init-cloud-sandbox-v0.3.1.bash` 仅把默认 project ZIP hash 从 zero 改为上述精确值；最终
  21,565 bytes、SHA-256 `ce31a32002aea46bbf3f9baf9a0e93451d24c3b3653952e425d1e1ff6960a5e8`；
- 本地完整回归、静态检查、Bash 语法、importer、ZIP self-containment、冻结输入复核与不可变
  v0.3.0 oracle 全部 PASS；Windows 的 12 个 POSIX-only case 仍诚实 SKIP，沿用已关闭 S2 的 Linux/
  Cloud 79/79 证据而不把它冒充最终字节验收；
- 两份精确 ZIP 构建保留在本地 S3-A 临时资产目录，未创建 commit、tag 或 Release，未 push、上传、
  重新下载、修改 live Cloud 或提升 rollback；
- 当时的下一关键边界是另行授权的 S3-B publication；它现已按下节完成。S3-C 仍必须复核发布页
  重新下载的两个精确资产并完成 final Cloud smoke。

### S3-B immutable publication — PASS / not yet accepted

- lightweight tag `v0.3.1` 精确指向 sealed source
  `9aa2148886e499f9f45594f7ae4f7681f1045de2`；
- Release：`https://github.com/keeptoy/pwf-codex-cloud-hooks-next/releases/tag/v0.3.1`，非 draft、
  非 prerelease，且显式未设为 `Latest`；
- 恰好两个资产：82,725-byte ZIP 与 21,565-byte external bootstrap；GitHub 服务端 digest 分别为
  `sha256:f097b040...31f9` 和 `sha256:ce31a320...a5e8`；
- S3-B 没有下载或执行公开资产，没有运行 final Cloud smoke，也没有提升 rollback；`Latest` 与
  accepted rollback 仍是 `v0.3.0`；
- S3-C 必须另行授权，并从上述 Release URL 重新下载两个资产，按完整 size/SHA 核对后再执行最终
  Cloud acceptance。不得用服务端 metadata 或本地保留副本代替消费路径下载证据。

## 13. S3-C：公开资产最终 Cloud smoke

本节只在活动计划明确授权 S3-C 后执行。维护者在 Codex Cloud 中选择本 runbook 分支并创建全新容器，
然后把下面脚本作为 setup script。Cloud 已负责装载指定仓库和分支；脚本不假设 checkout 存在
`origin` remote，也不重复 fetch、checkout 或 tag 校验。immutable tag/source 已由 S3-B 验证，本轮只
走公开资产消费路径：校验下载到的 bootstrap，再由它自行下载并校验固定 ZIP、install、doctor 和
verify。

### Codex Cloud checkout 与容器缓存

OpenAI 官方 [`Cloud environments`](https://developers.openai.com/codex/cloud/environments) 区分两条
生命周期：普通任务会先 checkout 用户选择的 branch/commit，再运行 setup script；启用容器缓存时，
缓存底座会在仓库默认分支运行 setup 并保存状态，恢复缓存后才 checkout 本次 chat 指定的分支，并只
运行可选 maintenance script。因此 setup 不得依赖当时的 workspace 分支、tag 或 Git remote 拓扑，
Fresh Release 验收也应创建新环境或先 Reset cache。

本节 setup 与仓库选择解耦：它只下载并校验固定公开 bootstrap；该 bootstrap 再下载并校验固定公开
ZIP，把 Skill、owned runtime 和 Managed requirements 安装到受管位置，并完成 doctor/verify。setup
阶段完成的是安装和静态健康验证；Hook 在后续 agent/Codex runtime 启动并读取 Managed requirements
时激活。第 4～8 节发生在 agent 阶段，此时 Cloud 已切到 chat 选择的分支；其中 F 使用 workspace
`install.js` 只做安装后的 doctor/inventory，不是 setup 的源码依赖。

```bash
set -Eeuo pipefail

readonly BOOTSTRAP_URL="https://github.com/keeptoy/pwf-codex-cloud-hooks-next/releases/download/v0.3.1/init-cloud-sandbox-v0.3.1.bash"
readonly BOOTSTRAP_SHA256="ce31a32002aea46bbf3f9baf9a0e93451d24c3b3653952e425d1e1ff6960a5e8"

S3C_DIR="$(mktemp -d)"
trap 'rm -rf -- "$S3C_DIR"' EXIT
BOOTSTRAP="$S3C_DIR/init-cloud-sandbox-v0.3.1.bash"

curl --fail --location --proto '=https' --tlsv1.2 \
  --output "$BOOTSTRAP" "$BOOTSTRAP_URL"
printf '%s  %s\n' "$BOOTSTRAP_SHA256" "$BOOTSTRAP" | sha256sum --check --strict

bash "$BOOTSTRAP" all

printf 'V031_S3C_BOOTSTRAP_SHA256=%s\n' "$BOOTSTRAP_SHA256"
printf 'V031_S3C_PUBLIC_RELEASE_SETUP=PASS\n'
```

setup PASS 后，在全新 task 中依次原样执行第 4～8 节 B、C、D、E1、E2、F。那些段落中的 `S2`
字符串是已经通过 S2 的固定黑盒 fixture ID，本轮不要临时重命名或改写提示词；只有同时具备本节
`V031_S3C_PUBLIC_RELEASE_SETUP=PASS`，其 B～F 结果才可归入 S3-C。

最终必须回传原始输出和以下摘要：

```text
V031_S3C_BOOTSTRAP_SHA256=ce31a32002aea46bbf3f9baf9a0e93451d24c3b3653952e425d1e1ff6960a5e8
V031_S3C_PUBLIC_RELEASE_SETUP=PASS
V031_S3C_B_FRESH=PASS
V031_S3C_C_BASELINE=PASS
V031_S3C_D_CANONICAL=PASS
V031_S3C_E1_TAIL_ACK=PASS
V031_S3C_E2_REAL_RESUME=PASS unsynced_messages=<actual>
V031_S3C_F_DOCTOR_INVENTORY_RESIDUE=PASS
V030_IMMUTABLE_ORACLE_UNCHANGED=PASS
```

任一项失败立即停止，不 repair、重发或替换资产。即使全部 PASS，也只把原始证据交回维护者；是否把
v0.3.1 提升为 accepted rollback 或 `Latest` 是后续显式决策，不由 Cloud task 自动完成。

## 14. S3-C 最终 Cloud 执行结果

维护者确认全新 Cloud 环境中的第 13 节 public Release setup 与第 4～8 节 B、C、D、E1、E2、F
全部 PASS。B～E 的完整控制台输出没有复制进本仓库；本节只记录维护者确认和已回传的 F 原始结果，
不反向编造缺失日志。

F 脚本在 `set -Eeuo pipefail` 下退出码为 0，并回传：

```text
{"action":"doctor","healthy":true,"repairable":false,"codex_home":"/opt/codex","skill_root":"/root/.agents/skills/planning-with-files","requirements_file":"/etc/codex/requirements.toml","managed":true,"events":["SessionStart","UserPromptSubmit"],"errors":[],"blockers":[]}
POST_RESUME_DOCTOR=PASS
INSTALLER_VERSION=0.3.1
INSTALLED_RUNTIME_FILES=11
MANAGED_POLICY=ADAPTER_ONLY
INSTALLED_RUNTIME_INVENTORY=["THIRD_PARTY_NOTICES.md", "compatibility-overlays-v1.json", "contracts/adapter-plan-context-request-v1.schema.json", "contracts/plan-context-result-v1.schema.json", "hook_adapter.py", "owned-catchup.py", "owned-plan.py", "upstream/inject-plan.sh", "upstream/ledger-summary.sh", "upstream/resolve-plan-dir.sh", "upstream/session-catchup.py"]
SNAPSHOT_LEFTOVERS=0
V031_S2_POST_RESUME=PASS
```

其中 `V031_S2_POST_RESUME` 是第 4～8 节复用的冻结 fixture ID，不表示误跑旧 S2 资产。结合第 13 节
public setup PASS、维护者确认的 B～E PASS，以及上面的 F 原始结果，v0.3.1 S3-C Cloud hard
acceptance 关闭为 PASS。该结论只接受已发布 v0.3.1 的公开资产消费路径；S3-C 本身没有自动提升
rollback。随后维护者另行授权的 promotion 见第 15 节；Product Phase 4 仍未授权。

## 15. rollback/Latest promotion 结果

维护者在 S3-C 关闭并完成证据 checkpoint 后，显式授权把 v0.3.1 提升为 production rollback 与
GitHub `Latest`。promotion 前置检查确认当时 `Latest=v0.3.0`，v0.3.1 非 draft/非 prerelease，且公开
bootstrap 与 ZIP 的名称、大小、SHA-256 均与 S3-A/S3-C 一致。

唯一外部写操作是把现有 v0.3.1 Release 标记为 `Latest`。后置核验结果：

```text
LATEST_BEFORE=v0.3.0
LATEST_AFTER=v0.3.1
V031_RELEASE_BYTES_UNCHANGED=PASS
V030_RELEASE_BYTES_UNCHANGED=PASS
V031_TAG=9aa2148886e499f9f45594f7ae4f7681f1045de2
V030_TAG=1454c9224c83d11c073b05baf6e536a11c3bb0e5
REMOTE_MAIN=bef919475b6ebc3d74c09f9664749664cf950537
```

完整 Release 指纹比较覆盖 name/body/tag、draft/prerelease 与两个资产的 ID、时间、名称、大小和 digest；
除 GitHub latest pointer 外无变化。v0.3.1 现为 production rollback/`Latest`，v0.3.0 降为不可变
immediate previous fallback，beta.2 保持更早的不可变 fallback oracle。该 lifecycle 变化不移动
remote main，不授权 Product Phase 4，也不允许从 tag 之后的治理分支重建或替换 v0.3.1 资产。
