# v0.4.0 Cloud hard acceptance

本文件描述 `v0.4.0` 的版本验收增量，并保留构成该稳定候选基础的 `v0.4.0-dev` F0～F3 exact evidence。
稳定执行协议、黑盒提示词、停止条件和 evidence schema 只见
[`Cloud hard acceptance template`](cloud-hard-acceptance-template.md)；P9-A 的 bootstrap使用64 位 zero hash并 fail closed；
P9-B已完成本地 exact ZIP SHA封印，但 sealed-source Cloud、公开资产和 Latest 结论必须在各自 gate实际完成后再写入。

<a name="v0-4-0-gate-status"></a>

## 版本内 gate 验收状态

| Gate | 状态 | 模板 / 证据入口 | 不授权边界 |
|---|---|---|---|
| F0 development identity / guardrails | `PASS` | package、Release identity 与 candidate/accepted 窗口已闭合 | 不代表 runtime foundation 完成 |
| F1A contract/source foundation | `PASS` | 下方 F1B Cloud run 使用的完整 candidate 已包含 F1A | 不自动授权新 runtime behavior |
| F1B inactive runtime foundation | `PASS` | 下方 F1B historical evidence | 不自动授权 F2A behavior |
| F2A local implementation | `PASS` | [local implementation lifecycle reconciliation](history/phase-4.4-f2a-smart-activation-discovery.md#phase-4-4-post-implementation-lifecycle-reconciliation) | 不等于 Linux/Cloud PASS |
| F2A Source/Candidate/no-live Cloud | `PASS` | [通用 Source/Candidate sequence](cloud-hard-acceptance-template.md#source-candidate-sequence)；[下方 F2A exact evidence](#v0-4-0-dev-f2a-source-candidate-evidence) | 不证明真实 Cloud opt-in lifecycle，不进入 F2B |
| F2B local autonomous implementation | `PASS` | [实施后 lifecycle reconciliation](history/phase-4.5-f2b-autonomous-activation-discovery.md#phase-4-5-post-implementation-lifecycle-reconciliation) | 不等于真实 Cloud activation lifecycle PASS |
| F2B Source/Candidate/no-live Cloud | `PASS` | [通用 Source/Candidate sequence](cloud-hard-acceptance-template.md#source-candidate-sequence)；[下方 F2B exact evidence](#v0-4-0-dev-f2b-source-candidate-evidence) | 不执行真实 activation lifecycle，不进入 F3 |
| F3 Discovery | `CONDITIONAL_GO_TO_F3A` | active planning 与 programme authority | 只完成路线/门槛设计，不执行真实 activation 或 rollback |
| F3A local lifecycle foundation | `PASS` | [Phase 4.6 implementation reconciliation](history/phase-4.6-f3-cloud-lifecycle-discovery.md#phase-4-6-post-implementation-status)；[F3 runbook](v0.4.0-dev-f3-cloud-lifecycle-runbook.md) | 只证明 source/repository foundation，不等于 Linux/Cloud 或 live PASS |
| F3A Source/Candidate/no-live Cloud | `PASS` | [通用 Source/Candidate sequence](cloud-hard-acceptance-template.md#source-candidate-sequence)；[下方 F3A exact evidence](#v0-4-0-dev-f3a-source-candidate-evidence) | 不创建真实 activation，不自动授权 F3B |
| F3B0 live-preflight Discovery | `CONDITIONAL_GO_TO_F3B1_PROTOCOL_MATERIALIZATION` | [Phase 4.7 decision](history/phase-4.7-f3b-live-preflight-discovery.md#phase-4-7-decision) | 只冻结双身份、true Fresh、验证链与退出条件，不创建 opt-in state |
| F3B1 protocol materialization / no-live dry run | `PASS` | [F3 runbook exact transaction/DAG/evidence](v0.4.0-dev-f3-cloud-lifecycle-runbook.md#f3-runtime-source-transaction)；[Phase 4.7 post-implementation](history/phase-4.7-f3b-live-preflight-discovery.md#phase-4-7-post-implementation-status) | 只证明 repository protocol 闭合；没有真实 Cloud/profile state |
| F3B2 smart live chain | `PASS` | [下方 F3B2 exact evidence](#v0-4-0-dev-f3b2-smart-live-evidence)；[冻结 operator guide](v0.4.0-dev-f3b2-smart-live-operator-guide.md#f3b2-post-run-status) | 只证明 smart 可逆 opt-in；不自动授权 autonomous、tamper、evidence closure 或 rollback |
| F3B3 autonomous live Discovery | `CONDITIONAL_GO_TO_F3B3_AUTONOMOUS_MATERIALIZATION` | [Phase 4.8 decision](history/phase-4.8-f3b3-autonomous-live-discovery.md#phase-4-8-decision) | 历史结论只冻结独立 `A_BASE`、零 ledger、tamper、re-attest/re-arm 与 operator/evidence 方案 |
| F3B3 autonomous materialization / repository-only audit | `PASS` | [Phase 4.8 post-implementation](history/phase-4.8-f3b3-autonomous-live-discovery.md#phase-4-8-post-implementation-status-f3b3)；[pre-run operator guide](v0.4.0-dev-f3b3-autonomous-live-operator-guide.md#f3b3-pre-run-status) | 只证明本地 exact DAG、refs、candidate 与执行协议闭合，不等于 Cloud live |
| F3B3 autonomous Cloud live | `PASS` | [下方 F3B3 exact evidence](#v0-4-0-dev-f3b3-autonomous-live-evidence)；[operator guide post-run](v0.4.0-dev-f3b3-autonomous-live-operator-guide.md#f3b3-post-run-status) | 只证明 exact candidate 的 autonomous lifecycle，不自动授权 F3B4/F3C 或 Release |
| F3B4 evidence-closure Discovery | `CONDITIONAL_GO_TO_F3B4_EVIDENCE_CLOSURE` | [Phase 4.9 decision](history/phase-4.9-f3b4-evidence-closure-discovery.md#phase-4-9-decision) | 只完成十份 evidence/ref/candidate/residue/retention 对账；不等于 aggregate F3B PASS |
| F3B4 evidence closure | `PASS` | [Phase 4.9 post-implementation](history/phase-4.9-f3b4-evidence-closure-discovery.md#phase-4-9-post-implementation-status-f3b4)；[下方 aggregate closure](#v0-4-0-dev-f3b4-aggregate-closure) | 只汇总已接受 live evidence；refs 保留，不等于 rollback 或 Release |
| F3C rollback Discovery | `CONDITIONAL_GO_TO_F3C1_ROLLBACK_PROTOCOL_MATERIALIZATION` | [Phase 4.10 decision](history/phase-4.10-f3c-rollback-discovery.md#phase-4-10-decision) | 历史 gate只冻结 disarm-first transaction、双 profile live 分工与 evidence 模型 |
| F3C1 rollback protocol / Linux no-live | `PASS` | [Phase 4.10 Linux/no-live acceptance](history/phase-4.10-f3c-rollback-discovery.md#phase-4-10-f3c1-linux-no-live-acceptance)；[operator guide post-no-live status](v0.4.0-dev-f3c-rollback-operator-guide.md#f3c-post-no-live-status) | production/Release bytes不变；未执行 Cloud rollback，不授权 F3C2 |
| F3C2 smart live rollback/recovery | `PASS` | [下方 smart live evidence](#v0-4-0-dev-f3c2-smart-live-evidence)；[operator guide post-run](v0.4.0-dev-f3c-rollback-operator-guide.md#f3c2-smart-post-run-status) | 不授权 F3C3/F3C4、ref mutation 或 Release |
| F3C3 autonomous live rollback/recovery | `PASS` | [下方 autonomous live evidence](#v0-4-0-dev-f3c3-autonomous-live-evidence)；[operator guide post-run](v0.4.0-dev-f3c-rollback-operator-guide.md#f3c3-autonomous-post-run-status) | 不授权 F3C4、ref mutation 或 Release |
| F3C4 aggregate-closure Discovery | `CONDITIONAL_GO_TO_F3C4_AGGREGATE_CLOSURE` | [Phase 4.11 decision](history/phase-4.11-f3c4-aggregate-closure-discovery.md#phase-4-11-decision) | 只完成四份 record、两项 negative、identity/ref/residue/lifecycle 对账；closure 与 ref cleanup 未授权 |
| F3C4 aggregate closure | `PASS` | [下方 aggregate closure](#v0-4-0-dev-f3c4-aggregate-closure)；[Phase 4.11 post-implementation](history/phase-4.11-f3c4-aggregate-closure-discovery.md#phase-4-11-post-implementation-status-f3c4) | F3C与 Phase 4功能基线闭合；refs保留；该 gate 当时停止在 Phase 9前 |
| v0.4.0 Phase 9 Release Discovery | `CONDITIONAL_GO_TO_PRE_SEAL_MATERIALIZATION` | [版本化 Phase 9 decision](history/phase-9-v0.4.0-release-discovery.md#phase-9-v0-4-0-decision) | 只冻结 P9-A～P9-F、stable identity与第二轮 retirement；不授权修改 Release输入、seal、Cloud、publication、promotion或 ref mutation |
| v0.4.0 Phase 9 P9-A pre-seal materialization | `PASS` | [P9-A implementation reconciliation](history/phase-9-v0.4.0-release-discovery.md#phase-9-v0-4-0-p9-a-post-implementation) | 只冻结 stable zero-hash candidate；不授权写 exact ZIP hash、sealed-source Cloud、publication、promotion或 ref mutation |
| v0.4.0 Phase 9 P9-B local seal | `LOCAL_SEAL_PASS / SEALED_SOURCE_CLOUD_PENDING` | [P9-B local seal reconciliation](history/phase-9-v0.4.0-release-discovery.md#phase-9-v0-4-0-p9-b-local-seal)；[sealed-source Cloud 操作入口](#v0-4-0-p9-b-sealed-source-cloud-operator) | exact ZIP/bootstrap已冻结；不代表 Cloud PASS，不授权 tag、publication、promotion或 ref mutation |

这张表同时保留 `v0.4.0-dev` 功能开发证据与 `v0.4.0` Release gate 状态。实际执行到哪一步、失败重试与
下一操作仍只写活动 task plan；本地已完成 gate可以记录 exact byte facts，但未完成的 Cloud/后继 gate不得预填 source HEAD或 PASS evidence。

<a name="v0-4-0-p9-b-local-seal-evidence"></a>

## P9-B local seal evidence

P9-B 从 clean P9-A commit `cb5da4b61899cd05f237bc3adcd3e09c8cd24bee`开始。写 bootstrap前后各自独立构建并
check两份 ZIP，结果始终为 22 entries、85,519 bytes、SHA-256
`24a412c19e220a60134547a18797fbd382a48fd5319a1f30a6d5c9b47bd53bb3`，证明外部 bootstrap封印没有改变 ZIP。
stable bootstrap只把默认 checksum从64位 zero hash替换为该 exact ZIP SHA；其自身 SHA-256冻结为
`4ae21c1fc99f52b1382543fac437096d4db1d3415cb40df578f29ed82cc4c64f`。

这只是本地字节封印，不是 sealed-source Cloud PASS。exact sealed-source HEAD必须以本地 seal commit本身为准，由维护者 push后
通过通用 Source/Candidate setup与 deep check回传；任何 Cloud失败若要求修改 ZIP input，必须回到 P9-A并重算两项资产身份。
当前结论为：

`P9_B_LOCAL_SEAL_PASS / SEALED_SOURCE_CLOUD_PENDING / STOP_BEFORE_P9_C / PUBLICATION_NOT_AUTHORIZED`

<a name="v0-4-0-p9-b-sealed-source-cloud-operator"></a>

## P9-B sealed-source Cloud 操作入口

本节回答“维护者现在具体怎么验收”，但不保存第二份 Bash 权威。长脚本和通用黑盒提示词始终从最终 checkout 的
[`Cloud hard acceptance template`](cloud-hard-acceptance-template.md#source-candidate-sequence) 读取；不要使用旧 `/tmp` 副本、
旧分支文档或聊天记录里的脚本。以下所有 `__PWF_P9B_EXPECTED_HEAD__` 都必须替换为第 1 步打印的真实 commit。

### 1. 本地确定、push 并核对 exact HEAD

先确认本地 `0.4.0` 工作树干净，再由维护者执行下面的 PowerShell。智能体不得代替维护者 push：

~~~powershell
git status --short --branch
$expectedHead = (git rev-parse HEAD).Trim()
if (git status --porcelain) { throw "P9-B push blocked: worktree is not clean" }
git push origin 0.4.0
$remoteLine = git ls-remote origin refs/heads/0.4.0
if (-not $remoteLine) { throw "P9-B remote branch was not found" }
$remoteHead = ($remoteLine -split '\s+')[0]
if ($remoteHead -ne $expectedHead) {
  throw "P9-B HEAD mismatch: local=$expectedHead remote=$remoteHead"
}
"PWF_P9B_EXPECTED_HEAD=$expectedHead"
~~~

最后一行就是本轮唯一允许使用的 sealed-source HEAD。文档中的旧 commit、P9-A 起点或先前 Cloud HEAD 都不能替代它。

### 2. 准备正式 Cloud 环境

在 Codex Cloud 环境设置中选择本仓库与 `0.4.0` 分支，并把实际 Node major 以纯数字环境变量
`PWF_ACCEPTANCE_NODE_MAJOR` 注入。若不知道该环境的 Node major，可先在一次 disposable fact-only task 中运行
`node -p "process.versions.node.split('.')[0]"`；取得数字后丢弃该 task，设置变量，再新建正式 Fresh task。探针结果不是
P9-B evidence，正式验收不能从探针会话续跑。

每个新 Cloud task 的第一条消息都要先附通用模板第 0.4 节的“Cloud task 验收权限前缀”，明确禁止自动修复、commit、push
和 PR。异步工具只返回 `session_id` 时必须继续轮询；没有明确最终 `exit_code` 只能报告 `INCOMPLETE/UNKNOWN`，不得猜 PASS
或 FAIL。

### 3. 第一轮运行 4.1 Source/Candidate setup

创建正式 Fresh Cloud task，把 `__PWF_P9B_EXPECTED_HEAD__` 替换后发送下列文字；实际 Bash 只能从
[`4.1 Source/Candidate setup`](cloud-hard-acceptance-template.md#source-candidate-setup) 提取：

~~~text
不要修改仓库文件，不要 commit、push、创建或更新 PR。先读取当前 checkout 的
docs/cloud-hard-acceptance-template.md，并遵守第 0.4 节权限前缀。

本轮 P9-B expected sealed-source HEAD 是 __PWF_P9B_EXPECTED_HEAD__。
先只读确认 git rev-parse HEAD 与该值完全相同、工作树干净；不一致立即停止并回报，不得 checkout、修复或创建 PR。
然后原样提取并执行该文档第 4.1 节唯一 Bash block。若工具返回 session_id 或没有最终 exit_code，持续轮询到明确退出码。

最终原样回传：最终 exit code、PWF_SC_RUNBOOK_HEAD、Linux tests/pass/fail/skipped、ZIP entries/size/SHA-256、
deterministic 双构建结果、install/doctor 与 adapter probe 结果，以及 PWF_SOURCE_CANDIDATE_SETUP=PASS 是否出现。
禁止把部分 stdout 或暂未出现 PASS 当成失败。
~~~

本轮必须同时满足：最终 exit code 为 0；`PWF_SC_RUNBOOK_HEAD` 等于 expected HEAD；Linux `fail=0` 且 `skipped=0`；
ZIP 为 22 entries、85,519 bytes、SHA-256
`24a412c19e220a60134547a18797fbd382a48fd5319a1f30a6d5c9b47bd53bb3`；并出现
`PWF_SOURCE_CANDIDATE_SETUP=PASS`。任一项不满足就停止，不进入 B～E。

### 4. 按固定顺序完成 B～E 黑盒链

4.1 PASS 后严格按下面顺序操作，不合并任务、不交换顺序，也不从旧聊天复制提示词：

| 顺序 | Cloud 操作 | 唯一提示词来源 | 本步重点 |
|---|---|---|---|
| 5.1 | 新建一个 task | [`5.1 B-SC post-install Resume`](cloud-hard-acceptance-template.md#blackbox-post-install-resume) | 证明 setup 安装在新 task 中真实可见 |
| 第 6 节 | 在 5.1 的同一 task 继续 | [`C canonical baseline`](cloud-hard-acceptance-template.md#blackbox-canonical-baseline) | 只创建模板规定的 canonical `.planning` fixture |
| 第 7 节 | 在同一 task 继续 | [`D canonical context`](cloud-hard-acceptance-template.md#blackbox-canonical-context) | 观察 canonical plan context |
| 8.1 | 在同一 task 继续，完成后离开 | [`E1 long tail`](cloud-hard-acceptance-template.md#blackbox-real-resume) | 建立 long-tail 状态；不是 real Resume |
| 8.2 | 从 Cloud UI 重新打开 8.1 的同一个 task | [`E2 real Resume`](cloud-hard-acceptance-template.md#blackbox-real-resume) | 必须真实观察 `SessionStart source=resume` |

第 6 节造成的 `.planning` 变化是模板允许的唯一仓库变化；其他步骤不得修改产品、测试、文档、refs 或远端状态。

### 5. E2 后运行 9.1 deep check

完成真实 Resume 后，在该 task 中替换 expected HEAD并发送下列文字；实际 verifier 只从
[`9.1 Source/Candidate deep check`](cloud-hard-acceptance-template.md#source-candidate-deep-check) 重新提取：

~~~text
不要修复、修改、commit、push 或创建 PR。本轮 P9-B expected sealed-source HEAD 是
__PWF_P9B_EXPECTED_HEAD__。

重新读取当前 checkout 的 docs/cloud-hard-acceptance-template.md，原样提取并执行第 9.1 节唯一 Bash block；不要使用
旧 /tmp 文件。若工具返回 session_id 或没有最终 exit_code，持续轮询到明确退出码。

最终原样回传：最终 exit code、PWF_WORKTREE_CHANGES、POST_RESUME_DOCTOR、manifest/release/bundle 的 schema、path、ID
与 installed root、INSTALLER_VERSION、RELEASE_ARTIFACT_ENTRIES、INSTALLED_RUNTIME_FILES、UPSTREAM_PRISTINE_FILES、
BUNDLE_INSTALLED_INVENTORY、MANAGED_POLICY、PWF_DEEP_CHECK_HEAD、SNAPSHOT_LEFTOVERS 和 PWF_SC_POST_RESUME。
不得根据缺少某一行或部分 stdout 推断最终状态。
~~~

### 6. P9-B PASS 与停止条件

只有下面事实同时成立，维护者才能把原始输出交回本仓库记录 P9-B Cloud PASS：

- 4.1 与 9.1 都取得明确最终 exit code 0；
- `PWF_SC_RUNBOOK_HEAD == PWF_DEEP_CHECK_HEAD == PWF_P9B_EXPECTED_HEAD`；
- Linux 零失败、零 skip，双构建一致，ZIP identity 与本节冻结的 22 / 85,519 / `24a412c…bb3` 完全相同；
- 5.1、6、7、8.1、8.2 的 Host 观察全部符合模板，且 8.2 是同一 task 的真实 Resume；
- doctor healthy，manifest-routed Release/bundle inventory authoritative，managed policy 为 adapter-only，snapshot leftovers 为 0；
- deep check明确输出 `PWF_SC_POST_RESUME=PASS`；
- 除第 6 节 canonical `.planning` fixture 外工作树无变化，Cloud 没有 commit、PR、push、自动修复或 ref mutation。

HEAD 不一致时丢弃该 task并从正确 checkout重跑；无最终 exit code时状态是 `INCOMPLETE/UNKNOWN`；环境或工具问题先分类后用
新的 Fresh环境重跑；任何修复若改变 ZIP input，必须退回 P9-A、重新计算 ZIP/bootstrap identity并重做 P9-B。即使全部 PASS，
本轮授权也只允许回补 evidence并关闭 P9-B，仍须停止在 P9-C之前。

<a name="v0-4-0-dev-f3c4-aggregate-closure"></a>

## F3C4 aggregate closure

F3C4没有执行第五轮 Cloud rollback，也没有产生新的 runtime或 asset身份。它只汇总已经分别接受的证据：

- F3C1 ref-aware Linux/no-live实际执行 smart与 autonomous两个 runtime-only revival negative；
- F3C2保留 smart `rollback` / `recovered`两份 live record；
- F3C3保留 autonomous `rollback` / `recovered`两份 live record；
- 四份 live record均证明 committed disarm、activation absent、legacy Fresh/real Resume、healthy doctor、verified backup、
  zero snapshot residue与 final exit code 0；
- exact protocol/current candidate/accepted asset/disarm refs沿用下方逐 gate证据，没有被 aggregate层重写或压扁。

第一轮 retirement review也已完成：disposable Cloud JSON、temporary ZIP/snapshot与 tamper workspace保持 RETIRED/ABSENT；
11个 validation refs因仍承担 exact side-branch reachability而 KEEP；operator guide迁移为当前列车 Phase 9的 Release-excluded
审计入口；rollback validator与 revival negative tests作为现行回归合同继续 KEEP。没有满足 DoD却仍被遗漏的无 owner对象，
也没有为了“完成清理”而批量删除 refs。

结论：

`F3C_ROLLBACK_PASS / SMART_AND_AUTONOMOUS_ROLLBACK_EVIDENCE_RECONCILED / PHASE_4_FUNCTIONAL_BASELINE_READY / STOP_BEFORE_PHASE_9`

本结论形成 `0.4.0`功能/候选基线，但不是 immutable publication、Published Release验收、Latest promotion或 accepted baseline；
当前 `0.4.0`列车的 standing Phase 9必须另行授权。

<a name="v0-4-0-dev-f3c3-autonomous-live-evidence"></a>

<a name="v0-4-0-dev-f3c3-autonomous-rollback-evidence"></a>

## F3C3 autonomous rollback/recovery evidence

维护者从 exact autonomous disarm HEAD `98b6f138497af244563541ec655a1111198f0c36` 创建独立 Cloud task，并完成
operator guide 的 `A_ROLLBACK` transaction、Fresh、同任务 real Resume、只读 verifier与 rollback evidence：

- protocol source HEAD 为 `12a359096ab1e376014476b77a6b0833a7a90b2e`；current candidate SHA-256 为
  `df60010402d1faf937d82a66007bd6a7d78f557b8da41a14ab283922c9a4494c`；
- immutable accepted 为 `v0.3.5`，source HEAD `5d01b55890c1da2a5088e2b991b152a9fb1c3f87`，ZIP SHA-256
  `7d351cfe0eaa60e93bc279645ed3f480dc9e83efdff1c6abf13c14d84c286f0b`；
- transaction经 current-owned uninstall后 clean-install accepted，最终 installed role/version为 `accepted` / `0.3.5`，
  backup计数 `3 → 6`并输出 `PWF_F3C_TRANSACTION_V1=PASS`；
- 新 Fresh task与同任务 real Resume均观察到对应 SessionStart canary和 UserPromptSubmit；Host保持 legacy delimiter与
  `=== recent progress ===`，未出现 nonce delimiter、`Plan-SHA256`、`=== ledger summary ===`、`=== RUN LEDGER ===`
  或 `entries: 0`；计划正文中的普通 nonce/SHA文本不作为 renderer泄漏；
- 第 8 节 verifier确认 exact HEAD、`autonomous_prepared`、activation absent、accepted v1 legacy context、doctor healthy、
  snapshot leftovers 0，并以明确 exit code 0结束；
- disposable evidence使用 `profile=autonomous`、`stage=rollback`、`installed_role=accepted`、
  `transition=current_uninstall_then_accepted_clean_install`，记录真实 startup/resume/UserPromptSubmit、backup verified与
  final exit code 0；helper输出 `F3_ROLLBACK_EVIDENCE_RECORD_V1=PASS`，validator exit code 0。

第一轮在 evidence review停止时的阶段状态为：

`F3C3_A_ROLLBACK_PASS / AUTONOMOUS_ACCEPTED_ROLLBACK_CONFIRMED / A_RECOVER_NOT_RUN / STOP_FOR_EVIDENCE_REVIEW`

维护者随后从同一 exact autonomous disarm HEAD创建独立 `A_RECOVER` Cloud task。Transaction先经过 immutable accepted
predecessor，再由 exact current candidate完成受支持的 forward recovery；Fresh与同任务 real Resume均观察到
startup/resume、UserPromptSubmit、legacy delimiter和 recent progress，且 nonce delimiter、`Plan-SHA256`、两个 ledger header
与 `entries: 0`全部未出现。第 8 节进一步确认：

- installed role/version为 `current` / `0.4.0-dev`；repository state仍为 `autonomous_prepared`；activation absent；
- v2 production result为 `context_emitted`、`inject=true`、effective profile `legacy`、advisory `null`；
- doctor healthy、backup verified、worktree clean、snapshot leftovers 0，verifier final exit code 0；
- recovered evidence使用 `profile=autonomous`、`stage=recovered`、
  `transition=accepted_to_current_exact_predecessor`，记录真实 startup/resume/UserPromptSubmit与 final exit code 0；
  helper输出 `F3_ROLLBACK_EVIDENCE_RECORD_V1=PASS`，validator exit code 0。

结论：`F3C3_AUTONOMOUS_LIVE_PASS / AUTONOMOUS_ROLLBACK_AND_EXACT_RECOVERY_CONFIRMED / STOP_BEFORE_F3C4`。
两轮共同证明 autonomous prepared残留在 accepted与 exact-current runtime下始终 inert；本结果不授权 F3C4、Release或
ref mutation。

<a name="v0-4-0-dev-f3c2-smart-live-evidence"></a>

<a name="v0-4-0-dev-f3c2-smart-rollback-evidence"></a>

## F3C2 smart rollback/recovery evidence

维护者从 exact smart disarm HEAD `c9275ba02073adb184cd73550c5b9f54c6f8178c` 创建独立 Cloud task，并完成
operator guide 的 transaction、Fresh、real Resume、只读 verifier 与 rollback evidence：

- protocol source HEAD 为 `12a359096ab1e376014476b77a6b0833a7a90b2e`；current candidate SHA-256 为
  `df60010402d1faf937d82a66007bd6a7d78f557b8da41a14ab283922c9a4494c`；
- immutable accepted 为 `v0.3.5`，source HEAD `5d01b55890c1da2a5088e2b991b152a9fb1c3f87`，ZIP SHA-256
  `7d351cfe0eaa60e93bc279645ed3f480dc9e83efdff1c6abf13c14d84c286f0b`；
- transaction 经 current uninstall 后 clean-install accepted，最终 installed role/version 为 `accepted` / `0.3.5`，
  backup 已验证；
- Fresh 与 real Resume 均观察到对应 SessionStart canary 和 UserPromptSubmit；Host 保持 legacy delimiter/recent progress，
  未出现 nonce delimiter 或 `Plan-SHA256`；
- 只读 verifier确认 `smart_prepared`、effective profile `legacy`、doctor healthy、snapshot leftovers 0；旧 v1 result没有
  `effective_profile` 字段时按 accepted markerless contract规范化为 legacy；
- evidence `stage=rollback`、`transition=current_uninstall_then_accepted_clean_install`，validator 输出
  `F3_ROLLBACK_EVIDENCE_RECORD_V1=PASS`，最终 exit code 0。

维护者随后从同一个 exact smart disarm HEAD 创建独立 `S_RECOVER` task。第 5 节 transaction仍先经过 immutable accepted
predecessor，再由 current installer执行 exact forward recovery；Fresh 与 real Resume 均观察到 startup/resume 和
UserPromptSubmit canary、legacy delimiter/recent progress，且没有 nonce delimiter 或 `Plan-SHA256`。第 8 节进一步确认：

- installed role/version 为 `current` / `0.4.0-dev`；repository state 仍为 `smart_prepared`；activation absent；
- v2 production result为 `context_emitted`、`inject=true`、effective profile `legacy`、advisory `null`、warnings空；
- doctor healthy、worktree clean、backup verified、snapshot leftovers 0，第 8 节最终 exit code 0；
- recovered evidence 使用 `stage=recovered`、`transition=accepted_to_current_exact_predecessor`，包含真实 startup/resume 与
  UserPromptSubmit，helper输出 `F3_ROLLBACK_EVIDENCE_RECORD_V1=PASS`，最终 exit code 0。

结论：`F3C2_SMART_LIVE_PASS / SMART_ROLLBACK_AND_EXACT_RECOVERY_CONFIRMED / STOP_BEFORE_F3C3`。这关闭 F3C2 smart
live，但不证明 autonomous rollback/recovery，不授权 F3C3、F3C4、Release 或 ref mutation。

<a name="v0-4-0-dev-f3c1-local-materialization"></a>

## F3C1 protocol/no-live acceptance

F3C1 已在 Release-excluded 层新增独立 rollback evidence helper、direct old-over-current no-mutation oracle、smart/autonomous
runtime-only revival negative 和自包含 operator guide。Windows 本地先通过结构、immutable publication oracle、Bash syntax
与治理测试；维护者随后从 GitHub 完整 clone，在 exact checkout
`cdc4a9eba7e7f1f2545723829ed1a6b4c76cb48b` 执行 operator guide第3节。

ref-aware 前置条件与实际结果：

- `v0.3.5` 解析到 `5d01b55890c1da2a5088e2b991b152a9fb1c3f87`；
- `v0.3.4` 解析到 `59a999f705701ec67463649e9424f3d059863c81`；
- 13 tests、13 pass、0 fail、0 skipped、final exit code 0；
- smart 与 autonomous 两个 runtime-only revival case均实际执行并通过。

此前仅含旧 commit objects但缺少发布 tags 的工作目录不满足 ref-aware 前置条件；该次报错不属于 product/test defect。

当前状态：

`F3C1_PROTOCOL_NO_LIVE_PASS / REF_AWARE_LINUX_ZERO_SKIP / CLOUD_ROLLBACK_NOT_RUN / STOP_BEFORE_F3C2`

本状态没有改写 candidate ZIP 输入，也没有执行 install切换、创建 ref或授权 F3C2/F3C3 live。

<a name="v0-4-0-dev-f3b4-aggregate-closure"></a>

## F3B4 aggregate closure

F3B2 smart 与 F3B3 autonomous 已分别保留 exact live evidence；Phase 4.9 又完成十份 records、Git refs、candidate、
Host/probe/doctor/final-exit provenance、markerless residue 与对象保留期的交叉对账。本次 closure 没有重跑 Cloud、修改
machine contract、复制 disposable JSON 或清理 validation refs。

最终状态为：

`F3B_LIVE_LIFECYCLE_PASS / SMART_AND_AUTONOMOUS_EVIDENCE_RECONCILED / STOP_BEFORE_F3C`

全部 validation refs 继续保留为 F3C/当前 Phase 9 evidence。F3C rollback、README/Release-byte 修改、seal、publication 与
promotion仍未授权。

<a name="v0-4-0-dev-f3b3-autonomous-live-evidence"></a>

## F3B3 autonomous live evidence

维护者在真实 Cloud 环境按冻结 operator guide 完成六个隔离 stage。共同 runtime source 为
`a6fa03159a442b917f893fc51a7e3ed45b37371a`；candidate 为 22 entries、85,533 bytes、SHA-256
`df60010402d1faf937d82a66007bd6a7d78f557b8da41a14ab283922c9a4494c`；active plan ID 为
`2026-08-16-phase-4-f3b3-autonomous-materialization`。

| Record | Exact workspace HEAD | Repository state / worktree | 实际 effective profile / advisory | 结果 |
|---|---|---|---|---|
| `prepared` | `d107c1cc53199415cc704553dafeab757060ae9e` | `autonomous_prepared` / clean | `legacy` / `null` | PASS |
| `armed` | `f43a744cbac7f7056d4efbf9b5cbd676bc1e4091` | `autonomous_armed` / clean | `autonomous` / `null` | Fresh + real Resume PASS |
| `tampered` | `f43a744cbac7f7056d4efbf9b5cbd676bc1e4091` | original armed HEAD / `tamper_only` | `null` / `state_unsafe` | canary-only refusal PASS |
| `disarmed` | `98b6f138497af244563541ec655a1111198f0c36` | `autonomous_prepared` / clean | `legacy` / `null` | PASS |
| `reprepared` | `5b20eb749c77dc1ac825202ca783dc7b8d938b58` | `autonomous_prepared` / clean | `legacy` / `null` | PASS |
| `rearmed` | `32b13b018176cd3bbaa15480864bf168754e5f67` | `autonomous_armed` / clean | `autonomous` / `null` | Fresh + real Resume PASS |

Initial task SHA/nonce 为 `415295db8617e87d8d63b94c891a7e1a1494f63024c96bba0993239564e9b552` /
`d7d00d0fcb799f3f`；reprepared/rearmed 为
`2f5cd2dcb0d5ce69fb000a97550096e1421e1cd6ad5569d570777cb744144878` / `c748f8700d4bfcd3`。两轮 autonomous
Host/probe 均使用本 stage delimiter/digest、输出 `=== RUN LEDGER ===` 与 `entries: 0`，不输出 raw recent progress 或
exclusion sentinel；rearmed verifier 另证明旧 nonce/digest 不出现。

Tamper 只在 exact `A_ARM` disposable checkout 修改 `task_plan.md`，实际 tampered SHA 为
`4264555784fb42dd0274f824c57a102bccc29a4f70217f83f2497e18b34a851a`。follow-up Host 只有 canary；probe 精确为
`outcome=invalid_request`、`inject=false`、`context=null`、`effective_profile=null`、`advisory=state_unsafe`。证据闭合后
整个 environment 已销毁，没有 cleanup、re-attest、repair、commit、push、PR 或 tamper ref。

六轮 setup/maintenance transaction、workspace preflight、production/tamper verifier、doctor、zero-residue check 和
`validateF3EvidenceRecord()` 均取得明确最终 exit code 0。Host 实际观察到 startup/UserPromptSubmit，armed 与 rearmed
分别观察到 mandatory `SessionStart source=resume`；各 record 的 cache 事实因不可可靠区分而诚实记录 `unknown`。

结论为：

`F3B3_AUTONOMOUS_LIVE_PASS / TAMPER_REFUSAL_AND_REATTEST_CONFIRMED / STOP_AND_REVIEW / STOP_BEFORE_F3B4`

结构 validator 只验证 record shape；Host canary/context、installed probe、Git identity、doctor 与最终 exit code共同承担
provenance。本 PASS 不自动授权 F3B4 evidence closure、F3C rollback、README/Release-byte 修改、seal、publication 或 promotion。

<a name="v0-4-0-dev-f3b2-smart-live-evidence"></a>

## F3B2 smart live evidence

维护者在真实 Cloud 环境按冻结 operator guide 顺序执行四个 exact workspace stage。共同 runtime source 为
`b37eea4706fed8d4e764f824eb75a3820f31c9be`；candidate 为 22 entries、85,533 bytes、SHA-256
`df60010402d1faf937d82a66007bd6a7d78f557b8da41a14ab283922c9a4494c`；active plan ID 为
`2026-08-14-phase-4-f3b2-smart-live`，task SHA-256 为
`ff952594cba55f5525d9e3ed3d8dc67c924df4da5e8603b0894a38cd3bcde96e`。

| Stage | Exact workspace HEAD | Repository state | 实际 effective profile | 结果 |
|---|---|---|---|---|
| `S_PREP` | `a39dc66c755ec19bf29504dc0844de995c6cf67c` | `smart_prepared` | `legacy` | PASS |
| `S_ARM` | `1058e704d5ab3496ab1a91a414c20c2e8fe58177` | `smart_armed` | `smart` | Fresh + real Resume PASS |
| `S_DISARM` | `c9275ba02073adb184cd73550c5b9f54c6f8178c` | `smart_prepared` | `legacy` | PASS |
| `S_REARM` | `6dea2225812939f7a5f9893f2ab90782742a264c` | `smart_armed` | `smart` | PASS |

Observed profile chain 为 `legacy → smart → legacy → smart`，直接证明 activation 可进入、删除后退出、重新加入后再进入。

四轮 setup/maintenance transaction 与 workspace preflight 均取得明确最终 exit code 0；真实 Host 输出观察到
SessionStart startup、UserPromptSubmit 与 plan context，`S_ARM` 另以 real Resume 观察到 SessionStart resume。连续对话中其他
stage 实际出现的 resume 只按事实记录，不替代 `S_ARM` 的强制 Resume 条件。各轮 production probe 与 expected profile
一致、`advisory=null`；doctor 均为 `healthy=true`、`managed=true`、`repairable=false`、无 errors/blockers；worktree clean，
snapshot leftovers 为 0。四份 disposable evidence JSON 均通过 `validateF3EvidenceRecord()`，没有可靠 cache 命中事实时诚实
记录 `cache_state=unknown`。

本证据证明 `F3B2_SMART_LIVE_PASS / REVERSIBLE_OPT_IN_LIFECYCLE_CONFIRMED / STOP_AND_REVIEW`。结构 validator 只证明
record 合同闭合；Host 原始输出、probe JSON、Git identity 与最终 exit code共同证明事件来源。F3B3 autonomous/tamper、
F3B4 evidence closure、F3C rollback、seal、publication 与 promotion仍未授权。

本次 closeout 只修改 Release-excluded 的 programme/acceptance/history/planning/tests（以及 Release-excluded
`ARCHITECTURE.md`）。`README.md` 是 ZIP entry，因此保留 Cloud 验收时的原始字节；若提升其 pre-live 摘要，必须形成新
candidate SHA 并重新走相称 Cloud gate，不能把上面的 `df6001…` 证据迁移到新字节。

<a name="v0-4-0-dev-f3b1-acceptance-delta"></a>

## F3B1 本地协议增量

- **协议物化：** setup 与 maintenance 使用同一 exact transaction，从显式 runtime source HEAD build/check 并核对外部
  expected candidate SHA 后安装；workspace lifecycle HEAD 作为另一身份独立记录。
- **repository dry run：** disposable Git repos 分别闭合 smart 与 autonomous prepare/arm/disarm/reprepare/re-arm 的
  direct-parent 和 exact-path DAG；没有在真实 active scope 或开发分支创建 profile/activation state。
- **负向与证据：** bounded autonomous tamper 只允许 disposable worktree 的单一 task path；exact JSON evidence validator
  同时约束字段集合、hash、stage/profile/worktree/Hook/advisory 关系与最终 exit code。
- **供应链边界：** 本 gate 只修改 tests/docs/planning；runbook/helper 不进入 runtime bundle 或 Release ZIP，不增加 Host
  event、managed writer、production parser 或 inventory authority。
- **停止线：** `F3B1_PROTOCOL_READY / NO_LIVE_STATE / STOP_BEFORE_F3B2`。本 PASS 不是 Cloud live evidence，不自动授权
  F3B2 smart、F3B3 autonomous、F3B4 evidence closure、F3C rollback 或 Release。

<a name="v0-4-0-dev-f3a-acceptance-delta"></a>

## F3A 验收增量与模板同步

- **代码/供应链增量：** production、contracts、runtime bundle、Release entries、installer 与 bootstrap 字节均不变；新增
  repository-only active-scope admission、disposable Git transition tests 与 [`F3 lifecycle runbook`](v0.4.0-dev-f3-cloud-lifecycle-runbook.md)。
- **portable 增量：** 通用 Source/Candidate Linux runner必须自动纳入 `f3-lifecycle-foundation.test.js`，证明 complete
  smart/autonomous state、partial/mismatch/link/unknown/inactive refusal、activation/disarm exact one-path commit 和 runbook
  Bash block syntax，且全 suite 零 fail/skip。
- **候选包增量：** F3A 文件全部位于 `tests/`、`docs/` 或 `.planning/`，必须继续被 Release contract 排除；候选 ZIP
  entries、installed inventory 和四个当前 pristine runtime 文件应保持由现行 machine authority 派生的既有集合。
- **B～E 黑盒：** 不变，继续证明 markerless legacy 默认。F3A no-live gate 禁止执行 runbook 中的 prepare/activate/disarm
  命令，禁止创建真实 `.mode`、`.nonce`、`.attestation`、`.pwf-codex-managed` 或 ledger。
- **完成判据：** exact checkout 上 Linux portable suite 零 fail/skip、deterministic candidate、bootstrap install、B～E、
  post-Resume doctor/deep check全部通过；工作树除模板 C 的 planning fixture 外无变化。随后才可把 F3A Cloud 行改为 `PASS`，
  写入 exact evidence并停下请求 F3B 授权。

本增量没有改写通用 B～E 提示词或 9.1 deep-check 脚本；云端执行仍必须遵守“无最终 exit code，不得判断成败”。

<a name="v0-4-0-dev-f3a-source-candidate-evidence"></a>

## F3A Source/Candidate evidence

维护者在全新 no-live Cloud 环境对 exact source `90d00de3f643defe566b1457064f46106ac791ae` 执行当前通用模板的
`4.1 → 5.1 → 6 → 7 → 8.1 → 8.2 → 9.1`，并确认整轮验收通过。本次回传只摘录 4.1 与 9.1 的完整关键结果；
B～E 的冗长原始 marker 未在本文件复制，也不补造动态文本或计数。

- portable/source：4.1 最终 exit code 0；149 tests，149 pass，0 fail，0 skipped；manifest/bundle authority PASS；
  setup 前后 source worktree clean；`PWF_SOURCE_CANDIDATE_SETUP=PASS`
- deterministic candidate：两次构建逐字节一致；22 entries；85,533 bytes；SHA-256
  `df60010402d1faf937d82a66007bd6a7d78f557b8da41a14ab283922c9a4494c`，与 F2B candidate 字节相同
- no-live behavior：B～E 沿用通用 markerless-legacy 黑盒，没有运行 F3 runbook 的 prepare/activate/disarm，也没有把
  repository fixture 或 source helper 冒充真实 opt-in evidence
- post-Resume：9.1 最终 exit code 0；doctor `healthy=true`、`repairable=false`、`managed=true`，events 精确为
  `SessionStart`、`UserPromptSubmit`，errors/blockers 为空；setup 与 deep-check HEAD 相同
- machine authority：manifest schema 4；Release contract `contracts/release-artifact-v2.json` /
  `PWF_RELEASE_ARTIFACT_V2`；bundle contract `contracts/runtime-bundle-v2.json` /
  `PWF_MANAGED_RUNTIME_BUNDLE_V2`；二者 schema 2；installed root `hooks/planning-with-files`
- installed state：installer `0.4.0-dev`；22 Release entries；12 installed runtime files；4 upstream pristine files；
  bundle inventory authoritative；adapter-only policy；零 snapshot residue；`POST_RESUME_DOCTOR=PASS` 与
  `PWF_SC_POST_RESUME=PASS`
- write boundary：9.1 只允许模板 C 的 planning fixture，因此输出 `PWF_WORKTREE_CHANGES=PLANNING_ONLY`；Cloud 模型没有
  修改 source、创建 commit 或 PR

结论：`F3A_SOURCE_CANDIDATE_NO_LIVE_CLOUD_PASS / STOP_BEFORE_F3B`。该结论把 repository admission、Git transition
fixture、版本化 runbook 静态合同、portable Linux、deterministic supply chain、安装与 markerless legacy 默认闭合到同一
exact source；它不证明 smart/autonomous 真实 prepare、activation、Fresh/UserPrompt/Resume、tamper、disarm/re-arm 或
rollback，不产生 Published Release identity，也不授权 F3B、F3C、seal、publication、promotion 或 baseline rotation。

## F2B 验收增量与模板同步

- **新增风险面：** adapter/runtime capability 现为 exact `[legacy, smart, autonomous]`；autonomous reader 增加
  profile-bound activation、task attestation、nonce、bounded JSONL ledger 与 post-render identity revalidation。
- **portable 增量：** 通用 Source/Candidate sequence 会动态纳入新增 tests，Linux 必须零 skip，直接覆盖 autonomous
  context、零 ledger、raw-progress no-read、invalid/budget/link/race/tamper、smart/legacy compatibility 和 `gate` denial。
- **供应链增量：** 仍由 manifest-routed runtime bundle v2 校验 adapter、owned-plan 和 request schema 的新 hash；entry
  数量、installed inventory、adapter-only policy 与 pristine upstream helper inventory 不新增第二份 authority。
- **B～E 黑盒：** 不变，继续证明 markerless legacy 默认；F2B no-live gate 不创建真实 activation state。真实 prepare、
  activation、disarm/re-arm、Fresh/Resume/cache 与 rollback 属 F3，当前禁止执行。
- **完成判据：** exact checkout 上 Linux portable suite 零 fail/skip、deterministic candidate、bootstrap install、B～E、
  post-Resume doctor/deep check 全部通过后，才把本表 F2B Cloud 行改为 `PASS` 并新增 exact evidence。

<a name="v0-4-0-dev-f2b-source-candidate-evidence"></a>

## F2B Source/Candidate evidence

维护者在全新 no-live Cloud 环境按当前通用模板完成 Source/Candidate setup、B～E markerless legacy 黑盒与
post-Resume deep check，并回传：

- exact source：`aeffc4d4c9e709ae59de2b193dabe5d092c5cb42`
- Linux portable suite：144 tests，144 pass，0 fail，0 skipped
- deterministic candidate ZIP：两次独立构建字节一致；22 entries；85,533 bytes
- candidate ZIP SHA-256：`df60010402d1faf937d82a66007bd6a7d78f557b8da41a14ab283922c9a4494c`
- candidate identity：package 与 Release contract 均为 `pwf-codex-cloud-hooks v0.4.0-dev`；bootstrap 保持在 ZIP 外
- setup：pinned planning-with-files v3.8.2、managed requirements TOML、Codex Hooks feature、installer doctor、
  SessionStart 与 UserPromptSubmit adapter probes 全部通过；`PWF_SOURCE_CANDIDATE_SETUP=PASS`
- install/doctor：安装根为 `/opt/codex`；managed runtime healthy/managed，零 errors/blockers；只注册
  `SessionStart` 与 `UserPromptSubmit`
- post-Resume authority：manifest schema 4 动态路由 `contracts/release-artifact-v2.json` / `PWF_RELEASE_ARTIFACT_V2`
  与 `contracts/runtime-bundle-v2.json` / `PWF_MANAGED_RUNTIME_BUNDLE_V2`；二者 schema 均为 2；installed root 为
  `hooks/planning-with-files`
- installed state：installer `0.4.0-dev`；22 Release entries；12 installed runtime files；4 upstream pristine files；
  bundle inventory authoritative；adapter-only policy；零 snapshot residue；`POST_RESUME_DOCTOR=PASS` 与
  `PWF_SC_POST_RESUME=PASS`
- identity/write boundary：setup 与 9.1 deep check HEAD 均为 exact source；Source/Candidate setup 前后工作树干净，
  9.1 只观察到模板 C 创建的 planning fixture，故 `PWF_WORKTREE_CHANGES=PLANNING_ONLY`；未创建 branch、commit 或 PR

结论：`F2B_SOURCE_CANDIDATE_CLOUD_PASS / STOP_BEFORE_F3`。该结论证明 F2B read-only autonomous consumer 的
portable Linux、供应链、安装与 markerless legacy 默认闭合；不证明真实 prepare/review/activation、Fresh/Resume/cache、
disarm/re-arm 或 rollback，不产生 Published Release identity，也不授权 F3、seal、publication、promotion 或 baseline
rotation。

### F2B asynchronous runner misclassification note

该轮 9.1 首次执行时，Cloud 命令工具在短暂 initial yield 后只返回了 `PWF_WORKTREE_CHANGES=PLANNING_ONLY`、doctor
JSON 与一个仍在运行的 session。Cloud 模型只读取该 stdout 分片，没有继续轮询，也没有取得最终 exit code，却错误声称
脚本以 exit code 1 失败。后继执行保留并轮询同一类异步 session，取得剩余 deep-check PASS 输出与明确 exit code 0，证明
脚本、Bash/Python heredoc、操作系统和安装状态均无故障；首次说法属于 **asynchronous tool-state misclassification**，
不是 product、script、platform 或 prompt-escaping defect，也不计为一次真实 gate failure。

因此通用模板已把“stdout 不是进程状态”加入每个 Cloud task 的强制权限前缀与 hard-stop 规则：有 session/running 或无
最终 exit code 时必须持续轮询；session 丢失只能报告 `INCOMPLETE/UNKNOWN`；禁止根据缺少 PASS marker 猜 FAIL，更禁止
声称工具未实际返回的 exit code。该治理适用于 test、build、install、download 和 deep check 等所有长命令，不为 PWF
脚本另建特殊分支。

<a name="v0-4-0-dev-f2a-acceptance-delta"></a>

## F2A 验收增量与模板同步

- **脚本同步：** Source/Candidate setup 与 post-Resume deep check 直接使用当前树通用模板的
  [`4.1`](cloud-hard-acceptance-template.md#source-candidate-setup)、
  [`9.1`](cloud-hard-acceptance-template.md#source-candidate-deep-check)；测试
  列表动态收集全部非 publication `*.test.js`，因此会自动执行 F2A smart、armed refusal、race、disarm、旧 marker
  inert 和真实 adapter/runtime 组合测试，不维护第二份手工文件清单。
- **提示词同步：** 模板 [`C`](cloud-hard-acceptance-template.md#blackbox-canonical-baseline) 已改为 structured
  canonical plan；本轮 plan ID 使用日期 + 8 位 run ID，避免同日循环测试覆盖旧 fixture，并明确不得创建
  `.pwf-codex-managed`、`.mode` 或其他 activation/profile 文件；
  [`D`](cloud-hard-acceptance-template.md#blackbox-canonical-context) 与
  [`E2`](cloud-hard-acceptance-template.md#blackbox-real-resume) 都必须同时观察
  `PWF_CLOUD_ACCEPTANCE_MARKERLESS_LEGACY_COMPLETED_V1` 和
  `PWF_CLOUD_ACCEPTANCE_MARKERLESS_LEGACY_ACTIVE_V1`。这两个 exact sentinel 是当前模板已同步到 F2A 的签名。
- **本 gate 要证明：** Linux portable suite `0 failed / 0 skipped`，candidate 双构建一致，安装/doctor/inventory/hash
  自洽，以及 Fresh canonical 与 real Resume 在 markerless workspace 中仍保持 legacy 默认。
- **本 gate 不证明：** 真实 smart activation、opt-out/re-arm、Fresh/Resume/cache 持久性或双向 rollback；这些仍属 F3。
- **完成写回：** 整条路线通过后，在本文件新增独立 F2A exact evidence 段；没有覆盖 F1B 证据，也没有让
  F1B 的旧 source/ZIP hash 冒充当前 F2A bytes。

### F2A test-harness deviation 与协议修正

首次 post-Resume 调用实际复用了退役的 v1 deep-check 脚本，因读取 `release-artifact-v1`、`runtime-bundle-v1` 与
`bundle.files` 失败；当前仓库 9.1 当时已经从 manifest 动态路由 v2 contract，并使用
`upstream_files/local_files/installed_contracts`，因此该失败分类为 **stale external acceptance script**，不是产品、
安装或 current template defect。旧脚本只读，未改变安装或 workspace；修正后的新 invocation 完整重跑并得到
doctor healthy、12 项 installed inventory 与 bundle authority 一致、4 个 upstream pristine、adapter-only policy、
零 snapshot residue 和 `PWF_SC_POST_RESUME=PASS`，证明修正后的 v2 deep-check 逻辑本身可通过。

该轮 Cloud 模型在发现旧脚本后自行修复并创建 PR，使 9.1 checkout HEAD 从原始 candidate 前移；它因此只保留为
诊断/功能证据，不作为最终 exact F2A gate 封账。后继模板已明确禁止 branch/commit/push/PR 与自动修复，只允许 C
段修改 canonical planning fixture；9.1 用简短 guard 拒绝 `.planning/` 之外的仓库改动，并打印当前 deep-check HEAD。
4.1 的 Source/Candidate HEAD 与 ZIP SHA 继续由最终 evidence writeback 对账，不再引入跨 task 临时状态协议。

模板不再输出自证式 `PWF_DEEP_CHECK_PROTOCOL` 口号，而是从 manifest 当前路由打印 release/bundle contract 的
path、id、schema 与 installed root。后继 Fresh run 已从同一 exact HEAD 回传 Linux suite、candidate identity、
B～E blackbox 与 9.1 摘要并闭合下方 F2A evidence；此前发生 HEAD 漂移的诊断轮不参与最终封账。

<a name="v0-4-0-dev-f2a-source-candidate-evidence"></a>

## F2A Source/Candidate evidence (historical gate instance)

维护者在全新 no-live Cloud 环境按当前通用模板完成 Source/Candidate setup、canonical baseline、
UserPromptSubmit、real Resume 与 post-Resume deep check，并回传：

- exact source：`31411b95d126ee9b27986fdcd72044f9474d3816`
- Linux portable suite：138 tests，138 pass，0 fail，0 skipped
- deterministic candidate ZIP：两次独立构建字节一致；22 entries；82,635 bytes
- candidate ZIP SHA-256：`7f1b1bd30d73011b0003d9c7e67e2df31bd302a08932c1302a83a84636ac3db4`
- setup：pinned planning-with-files v3.8.2、installer doctor、requirements TOML、SessionStart 与
  UserPromptSubmit adapter probe 全部通过；`PWF_SOURCE_CANDIDATE_SETUP=PASS`
- canonical fixture：`PWF_CLOUD_ACCEPTANCE_BASELINE_CREATED
  plan_id=2026-08-13-pwf-cloud-acceptance-v1-8e109d64`
- canonical context：canary、canonical marker、markerless legacy completed/active 双 sentinel、active plan、
  plan data、recent progress 与 planning context 全部 observed
- B～E no-live blackbox 路线按模板完成；维护者回传中省略了冗长原始段落，本文件不补造其中的动态计数
- post-Resume：`POST_RESUME_DOCTOR=PASS`；manifest schema 4 动态路由 Release v2 与 bundle v2；
  installer `0.4.0-dev`；22 Release entries；12 installed runtime files；4 upstream pristine files；bundle
  inventory authoritative；adapter-only policy；零 snapshot residue；`PWF_SC_POST_RESUME=PASS`
- identity/write boundary：9.1 HEAD 与 setup exact source 一致；`PWF_WORKTREE_CHANGES=PLANNING_ONLY`，未创建
  branch、commit 或 PR

结论：`F2A_SOURCE_CANDIDATE_CLOUD_PASS / STOP_BEFORE_F2B_F3`。该结论证明当前 F2A candidate 的 portable
Linux、供应链、安装与 markerless legacy 默认闭合；不证明真实 smart activation、opt-out/re-arm、cache/rollback，
不产生 Published Release identity，也不授权 F2B、F3、seal、publication、promotion 或 baseline rotation。

F1B Source/Candidate Cloud hard acceptance 已完成，并严格绑定下述 exact zero-hash candidate evidence。development bootstrap 使用 64 位 zero hash 并
fail closed；它只能通过显式 candidate ZIP URL/SHA override 安装，不代表 tag、Release 或 Published
Release 资产。

<a name="v0-4-0-dev-acceptance-scope"></a>

## F1B 验收增量（historical gate instance）

- F0 建立 `v0.4.0-dev` package/Release identity、外部 bootstrap 和 candidate/accepted 治理窗口；F1A
  原子迁移 source manifest schema 4、runtime bundle v2、Release artifact v2、adapter/四项 ABI
  placement 与 entry-owned mode。
- F1B 只轮转 plan request/result v2；catch-up 继续使用 v1。adapter producer 与 owned runtime capability
  均固定为 `allowed_profiles=[legacy]`，Host 输出、canary、上下文顺序和 managed-legacy 行为必须保持不变。
- `.mode` reader/normalizer 作为 inactive seam 随 runtime 安装，但 production 不得调用；nonce、attestation
  和 ledger reader 不得存在。Linux suite 必须零 skip 执行 future-profile refusal、legacy zero-read、unsafe
  mode/link/race 与旧 marker 不影响输出等证明。
- candidate installer 只接受当前完整状态或 transition contract 精确列出的 v0.3.5 predecessor。exact forward
  migration、tamper-before-write refusal 与 clean rollback 已由 ref-aware 本地 oracle 验证；tagless
  Source/Candidate Cloud 不伪造 refs，也不把 publication-only oracle 改写成 no-live 结果。
- exact current id/source inventory guard 继续拒绝未准入文件；Cloud setup 必须从当前 manifest/bundle 动态
  验证 v2 paths、hash、entry count、installed inventory 与 executable mode，不手填历史数量。
- 本次验收增量没有改写 B～E 黑盒提示词；它们继续验证真实 Host 行为与 legacy baseline 等价，F1B 的
  新增证明来自 Linux 零 skip suite 和 candidate 安装后的 exact deep check。

## F1B 执行范围（historical gate instance）

本次只执行通用模板的 Source/Candidate/no-live 路线，顺序固定为：

1. [`4.1 Source/Candidate setup`](cloud-hard-acceptance-template.md#source-candidate-setup)
2. `5.1 B-SC post-install Resume`
3. `6 C canonical baseline`
4. `7 D canonical UserPromptSubmit`
5. `8.1 E1 long-tail`
6. `8.2 E2 real Resume`
7. [`9.1 Source/Candidate deep check`](cloud-hard-acceptance-template.md#source-candidate-deep-check)

不得执行 Published Release 通道 `4.2`、`5.2`、`9.2`：当前 candidate 未 seal、未发布，也没有 Published
Release 身份。验收结果必须如实记录 setup、B～E 黑盒观测和 deep check；Linux test runner 必须显示
`0 failed`、`0 skipped`。

该 F1B development acceptance 不授予 F2A/F2B activation、managed token 生效、seal、publication、
promotion 或 baseline rotation。后续 gate 必须由新的活动 task plan 单独授权。

<a name="v0-4-0-dev-source-candidate-evidence"></a>

## F1B Source/Candidate evidence (historical gate instance)

维护者在全新 no-live Cloud 环境按 `4.1 → 5.1 → 6 → 7 → 8.1 → 8.2 → 9.1` 完整执行并回传：

- exact source：`fb61ca74517de399e80835a38ef4b2444d9e6468`
- Linux portable suite：133 tests，133 pass，0 fail，0 skipped
- deterministic candidate ZIP：两次独立构建字节一致；22 entries；80,800 bytes
- candidate ZIP SHA-256：`1866d56503a38dbd8269362c475c63ba1f4638ab7f69f66bad3b6ac9d5653dc9`
- setup：`PWF_SOURCE_CANDIDATE_SETUP=PASS`
- post-Resume：doctor `healthy=true`、`repairable=false`、`managed=true`，无 errors/blockers
- installed state：12 runtime files、4 pristine upstream files、bundle inventory authoritative、adapter-only policy
- residue：`SNAPSHOT_LEFTOVERS=0`，`PWF_SC_POST_RESUME=PASS`
- B～E no-live 黑盒路线已按模板完成；执行后 source worktree clean

结论：`SOURCE_CANDIDATE_CLOUD_PASS / F1_FOUNDATION_COMPLETE`。该结论只证明 exact zero-hash
Source/Candidate，不产生 Published Release identity，也不授权 F2A/F2B、seal、publication、promotion 或
baseline rotation。
