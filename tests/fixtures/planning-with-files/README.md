# Pinned upstream fixture

These files are copied from `OthmanAdi/planning-with-files` release `v3.8.2`
(commit `b04ffd9c8f9f93919649d197e5d4ec1bfc06fa14`) under its MIT license.
They keep installer integrity tests self-contained and must match
`upstream-manifest.json` exactly.
