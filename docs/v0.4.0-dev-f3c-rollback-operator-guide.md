<a name="f3c-operator-positioning"></a>

# v0.4.0-dev F3C disarm-first rollback：独立操作手册

本文是 F3C2/F3C3 后续真实 Cloud 验收的完整、独立入口，同时保存 F3C1 repository/Linux no-live 协议。
操作者不需要拼接 F3B2/F3B3 手册。F3C1 只物化本手册、测试与 evidence helper；当前尚未执行本手册中的
live transaction，也没有产生 `F3C_ROLLBACK_PASS`。

唯一支持链是：

```text
committed disarm + clean worktree
  → current-owned uninstall
  → immutable v0.3.5 clean install
  → current exact predecessor recovery
```

禁止让 v0.3.5 直接覆盖 current，禁止 armed/runtime-only rollback，禁止 installer 删除 workspace token，也禁止
根据 cache、stage 名或模型口述猜 installed role/profile。

<a name="f3c-one-minute-model"></a>

## 1. 一分钟理解四轮验收

smart 和 autonomous 各跑两轮：

| Stage | Workspace | 最终 installed role | 实际 profile | 要证明什么 |
|---|---|---|---|---|
| `S_ROLLBACK` | smart disarm | accepted `v0.3.5` | legacy | current 能先安全卸载，旧版只能 clean install |
| `S_RECOVER` | 同一 smart disarm | current `0.4.0-dev` | legacy | exact accepted predecessor 能正向迁回 current，token 不复活 |
| `A_ROLLBACK` | autonomous disarm | accepted `v0.3.5` | legacy | mode/nonce/attestation 保留但 inert，旧版只得到 legacy snapshot |
| `A_RECOVER` | 同一 autonomous disarm | current `0.4.0-dev` | legacy | current 恢复后 activation 仍不存在，autonomous 不复活 |

三层事实仍要分开：`WORKSPACE_STAGE` 是本轮做什么，`INSTALLED_ROLE` 是最终装了谁，`EFFECTIVE_PROFILE`
是 installed production 实际选择什么。`A_ROLLBACK` 可以同时是 autonomous prepared、accepted installed、legacy effective；
这三项不矛盾。

<a name="f3c-frozen-identities"></a>

## 2. 冻结身份

| 对象 | Exact identity |
|---|---|
| F3C protocol/runtime source | `12a359096ab1e376014476b77a6b0833a7a90b2e` |
| current package | `0.4.0-dev` |
| current candidate | 22 entries；85,533 bytes；SHA-256 `df60010402d1faf937d82a66007bd6a7d78f557b8da41a14ab283922c9a4494c` |
| accepted source | `5d01b55890c1da2a5088e2b991b152a9fb1c3f87` |
| accepted release | `v0.3.5` |
| accepted ZIP | `pwf-codex-cloud-hooks-v0.3.5.zip`；21 entries；77,800 bytes；SHA-256 `7d351cfe0eaa60e93bc279645ed3f480dc9e83efdff1c6abf13c14d84c286f0b` |
| smart disarm | `validation/v0.4.0-dev-f3b2-smart-disarm` / `c9275ba02073adb184cd73550c5b9f54c6f8178c` |
| autonomous disarm | `validation/v0.4.0-dev-f3b3-autonomous-disarm` / `98b6f138497af244563541ec655a1111198f0c36` |

Protocol source 是包含 F3C helper/no-live tests 的不可变 checkpoint；本手册本身在它之后提交，所以没有把最终文档
commit 自己写进自己的内容。两者构建出的 Release ZIP 输入相同。若 candidate SHA、accepted asset SHA 或任一 disarm
HEAD 不匹配，立即停止，不要更新 expected 值来迁就漂移。

<a name="f3c-no-live-gate"></a>

## 3. F3C1 repository/Linux no-live gate

在 ref-aware Linux checkout 运行：

```bash
set -Eeuo pipefail
node --test --test-reporter=tap \
  tests/f3c-rollback-protocol.test.js \
  tests/published-release-oracles.test.js
```

必须得到最终 `exit_code=0`。`f3c-rollback-protocol.test.js` 的 smart/autonomous runtime-only revival 两项在 Linux
必须实际执行，不得 skip：它们证明未 disarm 时，旧 runtime 临时只呈现 legacy，但 unchanged activation 在 current
runtime 回来后会重新激活。publication oracle 另外证明旧 installer direct-over-current 在 backup/write 前拒绝，且
runtime、requirements、backup inventory 完全不变。

<a name="f3c-cloud-environment"></a>

## 4. Cloud environment 变量

四轮使用同一份环境配置，只替换 `PWF_F3C_STAGE`：

```text
PWF_F3C_SOURCE_REPOSITORY=https://github.com/keeptoy/pwf-codex-cloud-hooks-next.git
PWF_F3C_PROTOCOL_SOURCE_HEAD=12a359096ab1e376014476b77a6b0833a7a90b2e
PWF_F3C_CURRENT_CANDIDATE_SHA256=df60010402d1faf937d82a66007bd6a7d78f557b8da41a14ab283922c9a4494c
PWF_F3C_ACCEPTED_ZIP_URL=https://github.com/keeptoy/pwf-codex-cloud-hooks-next/releases/download/v0.3.5/pwf-codex-cloud-hooks-v0.3.5.zip
PWF_F3C_ACCEPTED_ZIP_SHA256=7d351cfe0eaa60e93bc279645ed3f480dc9e83efdff1c6abf13c14d84c286f0b
PWF_F3C_CODEX_HOME=/opt/codex
PWF_F3C_MANAGED_REQUIREMENTS=/etc/codex/requirements.toml
PWF_F3C_SKILL_ROOT=/root/.agents/skills/planning-with-files
PWF_F3C_STAGE=S_ROLLBACK
```

`PWF_F3C_STAGE` 依次改为 `S_ROLLBACK`、`S_RECOVER`、`A_ROLLBACK`、`A_RECOVER`。每轮从对应 exact
disarm checkout 新建 task；不要让 Cloud 模型 checkout、commit、push、PR 或自动修复。

<a name="f3c-transaction"></a>

## 5. Setup 与 maintenance 共用的唯一 transaction

把下面同一 Bash block 原样放入 setup script 和 maintenance script。它先验证 workspace 已 committed disarm，再从
exact protocol source 构建 current ZIP、下载并校验 immutable accepted ZIP，最后按 stage 执行 transaction。

```bash
set -Eeuo pipefail

: "${PWF_F3C_SOURCE_REPOSITORY:?missing source repository}"
: "${PWF_F3C_PROTOCOL_SOURCE_HEAD:?missing protocol source HEAD}"
: "${PWF_F3C_CURRENT_CANDIDATE_SHA256:?missing current candidate SHA-256}"
: "${PWF_F3C_ACCEPTED_ZIP_URL:?missing accepted ZIP URL}"
: "${PWF_F3C_ACCEPTED_ZIP_SHA256:?missing accepted ZIP SHA-256}"
: "${PWF_F3C_CODEX_HOME:?missing CODEX_HOME}"
: "${PWF_F3C_MANAGED_REQUIREMENTS:?missing managed requirements path}"
: "${PWF_F3C_SKILL_ROOT:?missing Skill root}"
: "${PWF_F3C_STAGE:?missing F3C stage}"

EXPECTED_PROTOCOL_HEAD='12a359096ab1e376014476b77a6b0833a7a90b2e'
EXPECTED_CURRENT_SHA='df60010402d1faf937d82a66007bd6a7d78f557b8da41a14ab283922c9a4494c'
EXPECTED_ACCEPTED_SHA='7d351cfe0eaa60e93bc279645ed3f480dc9e83efdff1c6abf13c14d84c286f0b'
ACCEPTED_SOURCE_HEAD='5d01b55890c1da2a5088e2b991b152a9fb1c3f87'
PROJECT_ROOT="$(git rev-parse --show-toplevel)"

case "$PWF_F3C_STAGE" in
  S_ROLLBACK)
    PROFILE=smart; PLAN_ID='2026-08-14-phase-4-f3b2-smart-live'
    EXPECTED_WORKSPACE_HEAD='c9275ba02073adb184cd73550c5b9f54c6f8178c'
    EXPECTED_REPOSITORY_STATE=smart_prepared; FINAL_ROLE=accepted
    ;;
  S_RECOVER)
    PROFILE=smart; PLAN_ID='2026-08-14-phase-4-f3b2-smart-live'
    EXPECTED_WORKSPACE_HEAD='c9275ba02073adb184cd73550c5b9f54c6f8178c'
    EXPECTED_REPOSITORY_STATE=smart_prepared; FINAL_ROLE=current
    ;;
  A_ROLLBACK)
    PROFILE=autonomous; PLAN_ID='2026-08-16-phase-4-f3b3-autonomous-materialization'
    EXPECTED_WORKSPACE_HEAD='98b6f138497af244563541ec655a1111198f0c36'
    EXPECTED_REPOSITORY_STATE=autonomous_prepared; FINAL_ROLE=accepted
    ;;
  A_RECOVER)
    PROFILE=autonomous; PLAN_ID='2026-08-16-phase-4-f3b3-autonomous-materialization'
    EXPECTED_WORKSPACE_HEAD='98b6f138497af244563541ec655a1111198f0c36'
    EXPECTED_REPOSITORY_STATE=autonomous_prepared; FINAL_ROLE=current
    ;;
  *) printf 'unknown PWF_F3C_STAGE=%s\n' "$PWF_F3C_STAGE" >&2; exit 1 ;;
esac

test "$PWF_F3C_PROTOCOL_SOURCE_HEAD" = "$EXPECTED_PROTOCOL_HEAD"
test "$PWF_F3C_CURRENT_CANDIDATE_SHA256" = "$EXPECTED_CURRENT_SHA"
test "$PWF_F3C_ACCEPTED_ZIP_SHA256" = "$EXPECTED_ACCEPTED_SHA"
test "$(git -C "$PROJECT_ROOT" rev-parse HEAD)" = "$EXPECTED_WORKSPACE_HEAD"
test -z "$(git -C "$PROJECT_ROOT" status --porcelain=v1 --untracked-files=all)"
PLAN_DIR="$PROJECT_ROOT/.planning/$PLAN_ID"
test -d "$PLAN_DIR"
test ! -e "$PLAN_DIR/.pwf-codex-managed"

TRANSACTION_ROOT="${TMPDIR:-/tmp}/pwf-f3c-transaction"
case "$TRANSACTION_ROOT" in */pwf-f3c-transaction) ;; *) exit 1 ;; esac
rm -rf -- "$TRANSACTION_ROOT"
mkdir -p "$TRANSACTION_ROOT"
SOURCE_ROOT="$TRANSACTION_ROOT/source"
CURRENT_ZIP="$TRANSACTION_ROOT/current.zip"
CURRENT_EXTRACTED="$TRANSACTION_ROOT/current"
ACCEPTED_ZIP="$TRANSACTION_ROOT/accepted.zip"
ACCEPTED_EXTRACTED="$TRANSACTION_ROOT/accepted"

git init -q "$SOURCE_ROOT"
git -C "$SOURCE_ROOT" remote add origin "$PWF_F3C_SOURCE_REPOSITORY"
git -C "$SOURCE_ROOT" fetch --depth=1 origin "$PWF_F3C_PROTOCOL_SOURCE_HEAD"
git -C "$SOURCE_ROOT" checkout -q --detach FETCH_HEAD
test "$(git -C "$SOURCE_ROOT" rev-parse HEAD)" = "$EXPECTED_PROTOCOL_HEAD"
python3 "$SOURCE_ROOT/tools/import_upstream_runtime.py" check
python3 "$SOURCE_ROOT/tools/build_release.py" build --output "$CURRENT_ZIP"
python3 "$SOURCE_ROOT/tools/build_release.py" check --archive "$CURRENT_ZIP"
CURRENT_SHA="$(sha256sum "$CURRENT_ZIP" | awk '{print $1}')"
test "$CURRENT_SHA" = "$EXPECTED_CURRENT_SHA"

curl --fail --location --silent --show-error "$PWF_F3C_ACCEPTED_ZIP_URL" --output "$ACCEPTED_ZIP"
ACCEPTED_SHA="$(sha256sum "$ACCEPTED_ZIP" | awk '{print $1}')"
test "$ACCEPTED_SHA" = "$EXPECTED_ACCEPTED_SHA"

python3 - "$CURRENT_ZIP" "$CURRENT_EXTRACTED" "$ACCEPTED_ZIP" "$ACCEPTED_EXTRACTED" <<'PY'
import pathlib, sys, zipfile
for archive, destination in ((sys.argv[1], sys.argv[2]), (sys.argv[3], sys.argv[4])):
    target = pathlib.Path(destination)
    target.mkdir(parents=True, exist_ok=True)
    with zipfile.ZipFile(archive) as package:
        package.extractall(target)
PY
CURRENT_PACKAGE="$CURRENT_EXTRACTED/pwf-codex-cloud-hooks"
ACCEPTED_PACKAGE="$ACCEPTED_EXTRACTED/pwf-codex-cloud-hooks"
test "$(node -p "require('$CURRENT_PACKAGE/package.json').version")" = '0.4.0-dev'
test "$(node -p "require('$ACCEPTED_PACKAGE/package.json').version")" = '0.3.5'

REPOSITORY_STATE="$(PACKAGE_ROOT="$SOURCE_ROOT" PLAN_DIR="$PLAN_DIR" node <<'NODE'
const { validateActivePlanState } = require(process.env.PACKAGE_ROOT + '/tests/f3-lifecycle-helpers.js');
process.stdout.write(validateActivePlanState(process.env.PLAN_DIR));
NODE
)"
test "$REPOSITORY_STATE" = "$EXPECTED_REPOSITORY_STATE"

BACKUP_ROOT="$PWF_F3C_CODEX_HOME/backups/planning-with-files-hooks"
BACKUPS_BEFORE=0
test ! -d "$BACKUP_ROOT" || BACKUPS_BEFORE="$(find "$BACKUP_ROOT" -mindepth 1 -maxdepth 1 -type d | wc -l | tr -d '[:space:]')"

COMMON_ARGS=(--json --codex-home "$PWF_F3C_CODEX_HOME" --skill-root "$PWF_F3C_SKILL_ROOT" \
  --managed-requirements "$PWF_F3C_MANAGED_REQUIREMENTS")
node "$CURRENT_PACKAGE/install.js" install "${COMMON_ARGS[@]}"
node "$CURRENT_PACKAGE/install.js" doctor "${COMMON_ARGS[@]}"
node "$CURRENT_PACKAGE/install.js" uninstall "${COMMON_ARGS[@]}"
node "$ACCEPTED_PACKAGE/install.js" install "${COMMON_ARGS[@]}"
node "$ACCEPTED_PACKAGE/install.js" doctor "${COMMON_ARGS[@]}"

if test "$FINAL_ROLE" = current; then
  node "$CURRENT_PACKAGE/install.js" install "${COMMON_ARGS[@]}"
  node "$CURRENT_PACKAGE/install.js" doctor "${COMMON_ARGS[@]}"
  EXPECTED_INSTALLED_VERSION='0.4.0-dev'
else
  EXPECTED_INSTALLED_VERSION='0.3.5'
fi

INSTALLED_MANIFEST="$PWF_F3C_CODEX_HOME/hooks/planning-with-files/installed-manifest.json"
INSTALLED_VERSION="$(python3 - "$INSTALLED_MANIFEST" <<'PY'
import json, pathlib, sys
print(json.loads(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8'))['installer_version'])
PY
)"
test "$INSTALLED_VERSION" = "$EXPECTED_INSTALLED_VERSION"
BACKUPS_AFTER="$(find "$BACKUP_ROOT" -mindepth 1 -maxdepth 1 -type d | wc -l | tr -d '[:space:]')"
test "$BACKUPS_AFTER" -gt "$BACKUPS_BEFORE"
test ! -e "$PLAN_DIR/.pwf-codex-managed"
test -z "$(git -C "$PROJECT_ROOT" status --porcelain=v1 --untracked-files=all)"

cat >"$TRANSACTION_ROOT/facts.env" <<EOF
PWF_F3C_STAGE=$PWF_F3C_STAGE
PWF_F3C_PROFILE=$PROFILE
PWF_F3C_PLAN_ID=$PLAN_ID
PWF_F3C_WORKSPACE_DISARM_HEAD=$EXPECTED_WORKSPACE_HEAD
PWF_F3C_REPOSITORY_STATE=$REPOSITORY_STATE
PWF_F3C_INSTALLED_ROLE=$FINAL_ROLE
PWF_F3C_INSTALLED_VERSION=$INSTALLED_VERSION
PWF_F3C_BACKUPS_BEFORE=$BACKUPS_BEFORE
PWF_F3C_BACKUPS_AFTER=$BACKUPS_AFTER
PWF_F3C_ACCEPTED_SOURCE_HEAD=$ACCEPTED_SOURCE_HEAD
EOF

printf 'PWF_F3C_TRANSACTION_V1=PASS\n'
printf 'PWF_F3C_PROTOCOL_SOURCE_HEAD=%s\n' "$EXPECTED_PROTOCOL_HEAD"
printf 'PWF_F3C_CURRENT_CANDIDATE_SHA256=%s\n' "$CURRENT_SHA"
printf 'PWF_F3C_ACCEPTED_ZIP_SHA256=%s\n' "$ACCEPTED_SHA"
printf 'PWF_F3C_WORKSPACE_STAGE=%s\n' "$PWF_F3C_STAGE"
printf 'PWF_F3C_WORKSPACE_DISARM_HEAD=%s\n' "$EXPECTED_WORKSPACE_HEAD"
printf 'PWF_F3C_REPOSITORY_STATE=%s\n' "$REPOSITORY_STATE"
printf 'PWF_F3C_INSTALLED_ROLE=%s\n' "$FINAL_ROLE"
printf 'PWF_F3C_INSTALLED_VERSION=%s\n' "$INSTALLED_VERSION"
printf 'PWF_F3C_BACKUP_VERIFIED=PASS before=%s after=%s\n' "$BACKUPS_BEFORE" "$BACKUPS_AFTER"
```

setup/maintenance 是有意的 installed-state transaction，但不得写 workspace。必须持续轮询异步命令，直到工具返回明确
`exit_code`；没有最终 exit code 时只能报告 `INCOMPLETE/UNKNOWN`。

<a name="f3c-cloud-order"></a>

## 6. 四轮固定顺序

每轮只使用一种顺序：

1. 从对应 exact disarm HEAD 创建新的 Cloud task，并设置本轮 `PWF_F3C_STAGE`。
2. 等 setup/maintenance transaction 返回最终 `exit_code=0`。
3. 发送第 7 节第一条无工具提示词，保存真实 startup/UserPrompt Host 输出。
4. Resume 同一 task，发送第 7 节 Resume 提示词，保存真实 resume/UserPrompt 输出。
5. 运行第 8 节唯一只读 verifier，取得最终 `exit_code=0`。
6. 按第 9 节生成一份 `/tmp` evidence JSON 并用 checkpoint helper 校验。
7. 停止该 task；下一 stage 使用新的 exact checkout，不沿用 shell/cache 来证明身份。

<a name="f3c-host-prompts"></a>

## 7. Host 观察提示词

Fresh task 第一条消息只问：

```text
不要调用工具，不要修改文件，不要 commit/push/PR，也不要根据 stage 名猜结论。请原样列出本轮实际看到的全部
PWF_GLOBAL_HOOK_CANARY_V1 行；说明是否看到 [planning-with-files] ACTIVE PLAN、===BEGIN PLAN DATA===、
=== recent progress ===；说明是否出现任何 nonce delimiter 或 Plan-SHA256。最后只回传 expected workspace HEAD、
expected installed role；actual HEAD、actual installed role、actual effective profile 一律写 UNKNOWN，因为本条不运行只读检查。
```

然后真实 Resume 同一 task：

```text
不要调用工具，不要修改文件，不要 commit/push/PR。请原样列出本轮实际看到的全部 PWF_GLOBAL_HOOK_CANARY_V1 行；
确认是否真实出现 event=SessionStart source=resume 和 UserPromptSubmit；说明是否仍看到 legacy plan delimiter 与 recent
progress，以及是否错误出现 nonce delimiter 或 Plan-SHA256。actual installed role/profile 仍写 UNKNOWN，留给只读 verifier。
```

四轮都必须观察 startup、resume 与 UserPromptSubmit。文本外观只能作为 Host provenance，不能代替 installed probe。

<a name="f3c-read-only-verifier"></a>

## 8. 每轮最后的唯一只读 verifier

```bash
set -Eeuo pipefail

TRANSACTION_ROOT="${TMPDIR:-/tmp}/pwf-f3c-transaction"
test -f "$TRANSACTION_ROOT/facts.env"
. "$TRANSACTION_ROOT/facts.env"
PROJECT_ROOT="$(git rev-parse --show-toplevel)"
PLAN_DIR="$PROJECT_ROOT/.planning/$PWF_F3C_PLAN_ID"
test "$(git -C "$PROJECT_ROOT" rev-parse HEAD)" = "$PWF_F3C_WORKSPACE_DISARM_HEAD"
test -z "$(git -C "$PROJECT_ROOT" status --porcelain=v1 --untracked-files=all)"
test ! -e "$PLAN_DIR/.pwf-codex-managed"
test "$PWF_F3C_BACKUPS_AFTER" -gt "$PWF_F3C_BACKUPS_BEFORE"

if test "$PWF_F3C_INSTALLED_ROLE" = accepted; then
  PACKAGE_ROOT="$TRANSACTION_ROOT/accepted/pwf-codex-cloud-hooks"
  REQUEST_SCHEMA=1
else
  PACKAGE_ROOT="$TRANSACTION_ROOT/current/pwf-codex-cloud-hooks"
  REQUEST_SCHEMA=2
fi
OWNED_PLAN="$PWF_F3C_CODEX_HOME/hooks/planning-with-files/owned-plan.py"
test -x "$OWNED_PLAN"

if test "$REQUEST_SCHEMA" -eq 1; then
  PROBE_JSON="$(python3 - "$PROJECT_ROOT" "$PWF_F3C_PLAN_ID" <<'PY' | "$OWNED_PLAN"
import json, sys
print(json.dumps({
    'schema_version': 1, 'runtime': 'codex',
    'event': {'name': 'UserPromptSubmit', 'source': None, 'session_id': None, 'turn_id': None},
    'project': {'root': sys.argv[1], 'plan_id': sys.argv[2]},
    'policy': {'planning_enabled': True, 'behavior_profile': 'managed_legacy'},
    'output_budget': {'max_context_chars': 20000, 'max_plan_lines': 50, 'max_progress_lines': 20},
}))
PY
)"
else
  PROBE_JSON="$(python3 - "$PROJECT_ROOT" "$PWF_F3C_PLAN_ID" <<'PY' | "$OWNED_PLAN"
import json, sys
print(json.dumps({
    'schema_version': 2, 'runtime': 'codex',
    'event': {'name': 'UserPromptSubmit', 'source': None, 'session_id': None, 'turn_id': None},
    'project': {'root': sys.argv[1], 'plan_id': sys.argv[2]},
    'policy': {'planning_enabled': True, 'allowed_profiles': ['legacy', 'smart', 'autonomous'],
               'opt_in_protocol': 'codex-managed-v1'},
    'output_budget': {'max_context_chars': 20000, 'max_plan_lines': 50, 'max_progress_lines': 20},
}))
PY
)"
fi
printf '%s\n' "$PROBE_JSON"
PROBE_JSON="$PROBE_JSON" REQUEST_SCHEMA="$REQUEST_SCHEMA" python3 - <<'PY'
import json, os
value = json.loads(os.environ['PROBE_JSON'])
assert value['schema_version'] == int(os.environ['REQUEST_SCHEMA'])
assert value['outcome'] == 'context_emitted' and value['inject'] is True
context = value['context']
assert isinstance(context, str) and '===BEGIN PLAN DATA===' in context
assert '=== recent progress ===' in context
assert '===BEGIN-PLAN-DATA-' not in context and 'Plan-SHA256:' not in context
if value['schema_version'] == 2:
    assert value['effective_profile'] == 'legacy' and value['advisory'] is None
print('PWF_F3C_PRODUCTION_PROFILE=legacy')
PY

DOCTOR_JSON="$(node "$PACKAGE_ROOT/install.js" doctor --json \
  --codex-home "$PWF_F3C_CODEX_HOME" \
  --skill-root "$PWF_F3C_SKILL_ROOT" \
  --managed-requirements "$PWF_F3C_MANAGED_REQUIREMENTS")"
printf '%s\n' "$DOCTOR_JSON"
DOCTOR_JSON="$DOCTOR_JSON" EXPECTED_VERSION="$PWF_F3C_INSTALLED_VERSION" \
INSTALLED_MANIFEST="$PWF_F3C_CODEX_HOME/hooks/planning-with-files/installed-manifest.json" python3 - <<'PY'
import json, os, pathlib
doctor = json.loads(os.environ['DOCTOR_JSON'])
assert doctor['healthy'] is True and doctor['managed'] is True and doctor['repairable'] is False
assert doctor['errors'] == [] and doctor['blockers'] == []
manifest = json.loads(pathlib.Path(os.environ['INSTALLED_MANIFEST']).read_text(encoding='utf-8'))
assert manifest['installer_version'] == os.environ['EXPECTED_VERSION']
print('PWF_F3C_DOCTOR_AND_ROLE=PASS')
PY

SNAPSHOT_BASE="${TMPDIR:-/tmp}/pwf-codex-cloud-hooks/snapshots"
SNAPSHOT_LEFTOVERS=0
test ! -d "$SNAPSHOT_BASE" || SNAPSHOT_LEFTOVERS="$(find "$SNAPSHOT_BASE" -mindepth 1 -maxdepth 1 -type d -name 'pwf-snapshot-*' | wc -l | tr -d '[:space:]')"
test "$SNAPSHOT_LEFTOVERS" -eq 0
printf 'PWF_F3C_STAGE_READ_ONLY=PASS\n'
printf 'PWF_F3C_WORKSPACE_DISARM_HEAD=%s\n' "$PWF_F3C_WORKSPACE_DISARM_HEAD"
printf 'PWF_F3C_REPOSITORY_STATE=%s\n' "$PWF_F3C_REPOSITORY_STATE"
printf 'PWF_F3C_INSTALLED_ROLE=%s\n' "$PWF_F3C_INSTALLED_ROLE"
printf 'PWF_F3C_INSTALLED_VERSION=%s\n' "$PWF_F3C_INSTALLED_VERSION"
printf 'PWF_F3C_EFFECTIVE_PROFILE=legacy\n'
printf 'PWF_F3C_SNAPSHOT_LEFTOVERS=%s\n' "$SNAPSHOT_LEFTOVERS"
```

accepted v1 result没有 `effective_profile` 字段；它的 actual legacy 结论来自 exact installed version、v1
`managed_legacy` request、legacy delimiter/progress 断言共同归一化，不是由 stage 名推断。

<a name="f3c-evidence-records"></a>

## 9. 四份 rollback evidence

每轮保存 setup、Host、Resume、verifier、doctor 的原始输出和最终 exit code，然后写一份 disposable JSON：

```json
{
  "schema_version": 1,
  "profile": "smart",
  "stage": "rollback",
  "runtime_source_head": "12a359096ab1e376014476b77a6b0833a7a90b2e",
  "workspace_disarm_head": "c9275ba02073adb184cd73550c5b9f54c6f8178c",
  "current_version": "0.4.0-dev",
  "current_candidate_sha256": "df60010402d1faf937d82a66007bd6a7d78f557b8da41a14ab283922c9a4494c",
  "accepted_version": "v0.3.5",
  "accepted_source_head": "5d01b55890c1da2a5088e2b991b152a9fb1c3f87",
  "accepted_zip_sha256": "7d351cfe0eaa60e93bc279645ed3f480dc9e83efdff1c6abf13c14d84c286f0b",
  "installed_role": "accepted",
  "installed_version": "0.3.5",
  "repository_state": "smart_prepared",
  "activation_absent": true,
  "worktree": "clean",
  "session_start_sources": ["startup", "resume"],
  "user_prompt_observed": true,
  "hook_context": "legacy",
  "effective_profile": "legacy",
  "advisory": null,
  "doctor_healthy": true,
  "backup_verified": true,
  "transition": "current_uninstall_then_accepted_clean_install",
  "snapshot_leftovers": 0,
  "final_exit_code": 0
}
```

这是 shape 示例，不是可照抄的 PASS。`recovered` 必须使用 `installed_role=current`、`installed_version=0.4.0-dev`、
`transition=accepted_to_current_exact_predecessor`；autonomous 使用 autonomous disarm HEAD 和
`repository_state=autonomous_prepared`。只有实际观察到的 startup/resume/UserPrompt 才能写入。

使用 exact protocol checkpoint 中的 Release-excluded helper 校验：

```bash
TRANSACTION_ROOT="${TMPDIR:-/tmp}/pwf-f3c-transaction"
EVIDENCE_JSON="/tmp/pwf-f3c-<replace-stage>.json"
EVIDENCE_JSON="$EVIDENCE_JSON" PACKAGE_ROOT="$TRANSACTION_ROOT/source" node <<'NODE'
const fs = require('node:fs');
const { validateF3RollbackEvidenceRecord } = require(
  process.env.PACKAGE_ROOT + '/tests/f3-lifecycle-helpers.js');
const record = JSON.parse(fs.readFileSync(process.env.EVIDENCE_JSON, 'utf8'));
validateF3RollbackEvidenceRecord(record);
console.log('F3_ROLLBACK_EVIDENCE_RECORD_V1=PASS');
NODE
```

Helper 只验证 exact keys 和字段关系；它不能替代 immutable asset SHA、Host 原始输出、production probe、doctor 或最终
exit code。

<a name="f3c-stop-and-handoff"></a>

## 10. 停止条件与交接

以下任一情况立即停止并报告 `INCOMPLETE/FAIL`：

- workspace 不是 exact disarm HEAD、worktree 不干净或 activation 仍存在；
- current/accepted source、ZIP SHA、package version 或 installed manifest role不匹配；
- v0.3.5 direct-over-current 没有拒绝，或拒绝造成 backup/runtime/requirements mutation；
- rollback 未经过 current-owned uninstall，或 recovery 未经过 exact accepted predecessor；
- smart/autonomous 任一轮实际 profile不是 legacy，nonce/Plan-SHA 错误进入 context，或 activation 复活；
- doctor不健康、snapshot residue非零、Fresh/UserPrompt/real Resume缺失；
- 长命令没有明确最终 exit code。异步工具返回 session id时必须持续轮询；不得根据部分 stdout 判断成败。

F3C1 完成只允许进入维护者另行授权的 F3C2 smart live。F3C2 PASS 不自动授权 F3C3；两者都 PASS 后仍要单独进入
F3C4 aggregate closure。本手册不授权 ref mutation、Release、publication、promotion 或 Phase 9。

<a name="f3c-pre-run-status"></a>

## Pre-run status — 2026-08-17

`F3C1_PROTOCOL_MATERIALIZED / REPOSITORY_AND_LINUX_NO_LIVE_REQUIRED / CLOUD_ROLLBACK_NOT_RUN / STOP_BEFORE_F3C2`

当前只冻结了 protocol checkpoint、双身份 evidence shape、direct-downgrade refusal 和 runtime-only revival negative；
四轮 Cloud evidence 尚不存在，不能写 `F3C_ROLLBACK_PASS`。
