{
  "name": "pwf-codex-cloud-hooks",
  "version": "0.3.0-beta.3-dev",
  "private": true,
  "description": "Personal Codex Cloud global Hook installer for planning-with-files",
  "license": "MIT",
  "bin": { "pwf-codex-cloud-hooks": "install.js" },
  "scripts": { "test": "node --test tests/*.test.js" },
  "engines": { "node": ">=18" }
}
