# Progress: Phase 4 F2B autonomous activation Discovery

## 2026-08-13

- Maintainer authorized F2B Discovery after F2A Cloud closure; production implementation and live activation remain excluded.
- Recovered repository authorities and committed the Phase 4.4 post-acceptance governance reconciliation separately as `8f15e8a`.
- Reviewed current official OpenAI approval, Cloud and environment documentation. Confirmed that local approval and Cloud background
  review are different surfaces; no exact in-task Cloud consent callback was established.
- Created this dedicated Discovery scope with a lifecycle ledger and an explicit interface-only/defer fallback branch.
- Audited current schema/adapter/runtime reachability and pinned injector/ledger behavior. Confirmed autonomous is reserved but
  unreachable, while current exact state admission can be extended without importing a second Hook integration.
- Attempted to read upstream initializer/attester from the compact test fixture; only the admitted resolver/catch-up files exist there.
  Classified this as expected denied-source evidence and reused the earlier pinned-archive writer audit instead of downloading or
  admitting new executables.
- Closed the threat/lifecycle inventory. The existing smart token cannot authorize autonomous; the candidate autonomous commit point
  is exact `codex-managed-v1 autonomous\n`, written last after exact mode/state preparation. Attestation and nonce remain integrity/
  framing inputs rather than identity or secrets.
- Froze bounded ledger admission and a valid zero-ledger case. Autonomous never projects raw `progress.md`; invalid or changed state
  refuses with the existing result-v2 advisory vocabulary and no persistent correctness cache.
- Compared immediate activation, unreachable preimplementation, one coherent read-only implementation followed by F3, and defer.
  Selected `CONDITIONAL_GO_TO_F2B_READ_ONLY_IMPLEMENTATION`; the mandatory failure fallback is the current schema seam plus negative
  production guard, with no new dead parser or writer.
- Defined the Cloud prepare/review/follow-up-activate flow as an F3 hypothesis only. Local approval is not Cloud consent, and an
  external link/callback remains out of scope unless an authenticated official Host ABI appears.
- Added the Phase 4.5 historical milestone, history-index entry and minimal ROADMAP lifecycle/status handoff. Preserved the single
  historical macro entrance and the stable `F1 foundation complete` governance anchor after the boundary test caught both issues.
- `git diff --check` passed. Focused repository-boundary verification passed 9/9 after rerunning outside the Windows sandbox, where
  the first test-runner spawn attempt failed with `EPERM` before executing test bodies.
